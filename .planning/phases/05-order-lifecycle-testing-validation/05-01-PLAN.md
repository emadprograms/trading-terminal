---
wave: 1
depends_on: []
files_modified: ["playwright.config.ts", "tests/e2e/api-cleanup.ts", "tests/e2e/order-lifecycle.spec.ts", "tests/e2e/concurrency-locks.spec.ts"]
autonomous: true
---

# Phase 05 Plan 01: Order Lifecycle Testing & Validation

## Goal
Build a rigorous test suite to stress-test the order execution under all known and edge-case conditions, focusing strictly on functional correctness and data integrity without adding new capabilities.

## Requirements
- TEST-03: A rigorous order lifecycle test suite covers extreme latency edge cases, limit order cancellations, multi-chart event handling, and double alt logic.

## Tasks

<task>
  <id>1</id>
  <title>Configure Playwright for Toggleable Mock Environment</title>
  <read_first>playwright.config.ts</read_first>
  <action>Update `playwright.config.ts` to support a toggleable MSW mock environment. Read the `USE_MOCKS` environment variable. If `USE_MOCKS` is not set or false, retain the current behavior of forcing `x-bypass-mocks: true`. If `USE_MOCKS=true`, remove/omit the `x-bypass-mocks` header to allow MSW to intercept and mock the requests.</action>
  <acceptance_criteria>
    - `playwright.config.ts` contains logic to check `process.env.USE_MOCKS`.
    - When `process.env.USE_MOCKS` is 'true', `x-bypass-mocks` is not in the extraHTTPHeaders.
    - When `process.env.USE_MOCKS` is falsy, `x-bypass-mocks: true` is present in extraHTTPHeaders.
  </acceptance_criteria>
</task>

<task>
  <id>2</id>
  <title>Create API Cleanup Utility</title>
  <read_first>playwright.config.ts</read_first>
  <action>Create a new utility file `tests/e2e/api-cleanup.ts` that exports an async function `cleanupTestState(request: APIRequestContext)`. This function must directly call the Vercel API proxy to flatten all active positions (`DELETE /api/order/v1/positions`) and cancel all active limit/stop orders (`DELETE /api/order/v1/workingorders`). This avoids slow UI interactions for cleanup.</action>
  <acceptance_criteria>
    - `tests/e2e/api-cleanup.ts` exists and exports `cleanupTestState(request: APIRequestContext)`.
    - The function makes DELETE requests to `/api/order/v1/positions` and `/api/order/v1/workingorders`.
  </acceptance_criteria>
</task>

<task>
  <id>3</id>
  <title>Implement Order Lifecycle E2E Test Suite</title>
  <read_first>tests/e2e/api-cleanup.ts, .planning/phases/05-order-lifecycle-testing-validation/05-RESEARCH.md</read_first>
  <action>Create `tests/e2e/order-lifecycle.spec.ts`. Implement E2E tests for standard limit/market order placement and cancellation. Use `cleanupTestState` in a `beforeEach` hook to ensure a clean slate. Explicitly verify that limit orders can be cancelled and don't get permanently stuck (ORDER-02). Assert both final UI state and the number of network payloads fired (POST/DELETE to /api/order/v1/*).</action>
  <acceptance_criteria>
    - `tests/e2e/order-lifecycle.spec.ts` exists and imports `cleanupTestState`.
    - Contains tests for market and limit order placement.
    - Contains tests for limit order cancellation, verifying UI update and network API call.
    - `npx playwright test tests/e2e/order-lifecycle.spec.ts` exits 0.
  </acceptance_criteria>
</task>

<task>
  <id>4</id>
  <title>Implement Concurrency and Lock E2E Test Suite</title>
  <read_first>tests/e2e/api-cleanup.ts, .planning/phases/05-order-lifecycle-testing-validation/05-RESEARCH.md</read_first>
  <action>Create `tests/e2e/concurrency-locks.spec.ts`. Implement "impossible speed" tests (zero delay) to stress test Double Alt (flattenHalfSymbol) locking and multi-chart `alt+q` event handling. Use `page.route` to simulate network latency, while spamming `page.keyboard.press('Alt')` or rapid instrument switching before `alt+q`. Verify via request counts (`page.on('request')` or `page.route()`) that exactly ONE valid API request is fired to `/api/order/v1/positions` per logical user action, ensuring no ghost orders (AUDIT-01, ORDER-03, TEST-03).</action>
  <acceptance_criteria>
    - `tests/e2e/concurrency-locks.spec.ts` exists.
    - Contains test that spams Double Alt and asserts network request count === 1.
    - Contains test that triggers `alt+q` rapidly across multi-chart interactions and asserts network request count === 1.
    - `npx playwright test tests/e2e/concurrency-locks.spec.ts` exits 0.
  </acceptance_criteria>
</task>

## Verification Criteria
- [ ] `USE_MOCKS` toggle correctly conditionally applies `x-bypass-mocks` in Playwright config.
- [ ] API cleanup utility successfully resets state before tests.
- [ ] `order-lifecycle.spec.ts` passes against the deployed environment.
- [ ] `concurrency-locks.spec.ts` correctly captures network request counts and passes against the deployed environment.
- [ ] No subjective or incomplete tests remaining.

## must_haves
- All E2E specs MUST use the API cleanup utility before each test.
- Every test MUST assert network payload counts, not just UI state.
- The toggle for the MSW mocked environment MUST default to the live proxy behavior if not explicitly set.

## Artifacts this phase produces
- `tests/e2e/api-cleanup.ts` (File)
- `tests/e2e/order-lifecycle.spec.ts` (File)
- `tests/e2e/concurrency-locks.spec.ts` (File)
- `cleanupTestState` (Function in tests/e2e/api-cleanup.ts)

<threat_model>
- **Threat**: Overloading the live proxy / Capital.com demo API leading to rate limiting.
  **Mitigation**: Reusing test account, test runs constrained by environment controls, specific cleanup prevents account state pollution.
- **Threat**: Test code inadvertently introduced to production bundles.
  **Mitigation**: Test utilities and specs strictly confined to the `tests/e2e/` directory, excluded from typical production build targets.
</threat_model>
