---
wave: 1
depends_on: []
files_modified: ["playwright.config.ts", "tests/e2e/api-cleanup.ts", "tests/e2e/order-lifecycle.spec.ts", "tests/e2e/concurrency-locks.spec.ts"]
autonomous: true
---

# Phase 05 Plan 01: Order Lifecycle Testing & Validation

## Goal
Build a rigorous test suite to stress-test the order execution under all known and edge-case conditions, focusing strictly on functional correctness and data integrity without adding new capabilities.

## Requirements
- TEST-03: A rigorous order lifecycle test suite covers extreme latency edge cases, limit order cancellations, multi-chart event handling, and double alt logic.

## Tasks

<task>
  <id>1</id>
  <title>Configure Playwright for Mock Environment and Sequential Execution</title>
  <read_first>playwright.config.ts</read_first>
  <action>Update `playwright.config.ts` to support a toggleable MSW mock environment and sequential execution. Read the `USE_MOCKS` environment variable. If `USE_MOCKS` is not set or false, retain the current behavior of forcing `x-bypass-mocks: true`. If `USE_MOCKS=true`, remove/omit the `x-bypass-mocks` header. Set `workers: 1` explicitly in the configuration to enforce sequential execution and prevent race conditions with the single test account.</action>
  <acceptance_criteria>
    - `playwright.config.ts` sets `workers: 1` to ensure sequential execution.
    - `playwright.config.ts` contains logic to check `process.env.USE_MOCKS`.
    - When `process.env.USE_MOCKS` is 'true', `x-bypass-mocks` is not in the extraHTTPHeaders.
    - When `process.env.USE_MOCKS` is falsy, `x-bypass-mocks: true` is present in extraHTTPHeaders.
  </acceptance_criteria>
</task>

<task>
  <id>2</id>
  <title>Create API Cleanup Utility</title>
  <read_first>tests/e2e/api-cleanup.ts, playwright.config.ts, .planning/phases/05-order-lifecycle-testing-validation/05-RESEARCH.md</read_first>
  <action>Create a new utility file `tests/e2e/api-cleanup.ts` that exports an async function `cleanupTestState(page: Page, request: APIRequestContext)`. The tests will rely on an existing global setup for authentication state. Ensure the function checks for `CST` and `X-SECURITY-TOKEN` headers from the client application state (`localStorage` or browser context); if missing, explicitly throw an error "Missing authentication tokens for cleanup". The function must directly call the Vercel API proxy with these headers to flatten active positions (`DELETE /api/order/v1/positions`) and cancel active limit/stop orders (`DELETE /api/order/v1/workingorders`). Add strict error handling: throw an error immediately if the `DELETE` requests return non-2xx status codes.</action>
  <acceptance_criteria>
    - `tests/e2e/api-cleanup.ts` exists and exports `cleanupTestState(page: Page, request: APIRequestContext)`.
    - The function throws an error if auth headers are missing before making requests.
    - The function makes authenticated DELETE requests to `/api/order/v1/positions` and `/api/order/v1/workingorders`.
    - The function throws an error if the API responses have a non-2xx status code.
  </acceptance_criteria>
</task>

<task>
  <id>3</id>
  <title>Implement Order Lifecycle E2E Test Suite</title>
  <read_first>tests/e2e/order-lifecycle.spec.ts, tests/e2e/api-cleanup.ts, .planning/phases/05-order-lifecycle-testing-validation/05-RESEARCH.md</read_first>
  <action>Create `tests/e2e/order-lifecycle.spec.ts`. Implement E2E tests for standard limit/market order placement and cancellation. Use `cleanupTestState` in a `beforeEach` hook to ensure a clean slate. Explicitly verify that limit orders can be cancelled and don't get permanently stuck. Assert both final UI state and the number of network payloads fired (`POST` or `DELETE` to `/api/order/v1/*`) to ensure exactly one payload per action.</action>
  <acceptance_criteria>
    - `tests/e2e/order-lifecycle.spec.ts` exists and imports `cleanupTestState`.
    - Contains tests for market and limit order placement.
    - Contains tests for limit order cancellation, verifying UI update and network API call count === 1.
    - `npx playwright test tests/e2e/order-lifecycle.spec.ts` exits 0.
  </acceptance_criteria>
</task>

<task>
  <id>4</id>
  <title>Implement Concurrency and Lock E2E Test Suite</title>
  <read_first>tests/e2e/concurrency-locks.spec.ts, tests/e2e/api-cleanup.ts, .planning/phases/05-order-lifecycle-testing-validation/05-RESEARCH.md</read_first>
  <action>Create `tests/e2e/concurrency-locks.spec.ts`. Implement a test matrix with two passes: "impossible speed" (zero delay, simulated via `page.route` latency) and "realistic speed" (human delays). For the impossible speed tests, require `USE_MOCKS=true` to isolate network spam from the live API. Spam `page.keyboard.press('Alt')` or rapid instrument switching before `alt+q`. For the realistic speed tests, verify normal UI usage with human delays without mock restrictions. In all tests, verify via request counts (`page.on('request')`) that exactly ONE valid API request is fired to `/api/order/v1/positions` per logical user action, and ensure the UI correctly reflects the final state without ghost orders.</action>
  <acceptance_criteria>
    - `tests/e2e/concurrency-locks.spec.ts` exists.
    - Contains "realistic speed" tests simulating human interaction delays.
    - Contains "impossible speed" tests that spam inputs (Double Alt and multi-chart alt+q) with simulated latency.
    - Tests assert network request count === 1 and assert UI reflects the final state without ghost orders.
    - "Impossible speed" tests check for or run conditionally on `process.env.USE_MOCKS === 'true'`.
    - `npx playwright test tests/e2e/concurrency-locks.spec.ts` exits 0.
  </acceptance_criteria>
</task>

## Verification Criteria
- [ ] `USE_MOCKS` toggle correctly conditionally applies `x-bypass-mocks` in Playwright config.
- [ ] Playwright config enforces sequential execution (`workers: 1`).
- [ ] API cleanup utility successfully resets state before tests and handles missing auth/HTTP errors strictly.
- [ ] `order-lifecycle.spec.ts` passes against the deployed environment.
- [ ] `concurrency-locks.spec.ts` runs both realistic and impossible speed tests and passes.
- [ ] No subjective or incomplete tests remaining.

## must_haves
- **D-03:** All E2E specs MUST use the API cleanup utility before each test.
- **D-04:** Every test MUST assert network payload counts, not just UI state.
- **D-01:** The toggle for the MSW mocked environment MUST default to the live proxy behavior if not explicitly set.
- **D-02:** The concurrency test suite MUST run both realistic and impossible speed (zero delay) passes to stress the system's locking mechanisms.
- The test runner MUST execute sequentially (`workers: 1`) to prevent test collisions.

## Artifacts this phase produces
- `tests/e2e/api-cleanup.ts`
- `tests/e2e/order-lifecycle.spec.ts`
- `tests/e2e/concurrency-locks.spec.ts`
- `cleanupTestState`

<threat_model>
- **Threat**: Overloading the live proxy / Capital.com demo API leading to rate limiting.
  **Mitigation**: Reusing test account, test runs constrained by environment controls, specific cleanup prevents account state pollution. "Impossible speed" tests are isolated to `USE_MOCKS=true`.
- **Threat**: Test code inadvertently introduced to production bundles.
  **Mitigation**: Test utilities and specs strictly confined to the `tests/e2e/` directory, excluded from typical production build targets.
- **Threat**: Test state collisions leading to flaky test results and corrupted test accounts.
  **Mitigation**: Playwright forced to `workers: 1` to ensure sequential execution.
</threat_model>
