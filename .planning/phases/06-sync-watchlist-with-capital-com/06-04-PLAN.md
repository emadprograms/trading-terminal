---
wave: 4
depends_on: ["06-03"]
files_modified: ["tests/e2e/watchlist-sync.spec.ts"]
autonomous: true
---

# Phase 06 Plan 4: Testing & Verification

## Goal
Verify the real data flows of the watchlist synchronization using Playwright E2E tests against the proxy endpoints, including failure scenarios and test state teardown.

## must_haves
- [ ] Playwright E2E tests are implemented validating the Watchlist Sync flow against the live Capital.com API via the application proxy.
- [ ] Explicit teardown/cleanup logic ensures the live account is not polluted with test symbols.
- [ ] Failure scenarios (e.g., 500 server error) are tested to ensure the UI error toast appears.

## Artifacts this phase produces
- New File: `tests/e2e/watchlist-sync.spec.ts`

## Tasks

<task>
  <read_first>
    - tests/e2e/critical-path.spec.ts
  </read_first>
  <action>
    Create a new Playwright test file `tests/e2e/watchlist-sync.spec.ts`.
    Write a "happy path" test: open the watchlist, add a test symbol (e.g., `AAPL`), click the Sync button, and verify the network requests to `/api/watchlist` succeed.
    Implement an `afterAll` or `test.afterEach` block to explicitly clean up any symbols added during the test via direct API calls to the proxy or remote.
  </action>
  <acceptance_criteria>
    - `tests/e2e/watchlist-sync.spec.ts` executes successfully.
    - The test cleans up the Capital.com watchlist state in an `afterEach` or `afterAll` hook to prevent pollution.
  </acceptance_criteria>
</task>

<task>
  <read_first>
    - tests/e2e/watchlist-sync.spec.ts
  </read_first>
  <action>
    Add a "failure scenario" test case in `tests/e2e/watchlist-sync.spec.ts`.
    Use Playwright's network interception (`page.route`) to intercept the POST/PUT request to `/api/watchlist` and mock a 500 error response.
    Click the Sync button and assert that the error toast notification appears in the UI.
  </action>
  <acceptance_criteria>
    - The failure test correctly intercepts the network request and forces an error.
    - The test asserts the presence of the error toast in the DOM.
  </acceptance_criteria>
</task>

## Verification
- Execution of `tests/e2e/watchlist-sync.spec.ts` passes against the deployed/local proxy.
- Test account is free of test symbols after execution.
