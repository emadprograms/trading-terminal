---
phase: 06-sync-watchlist-with-capital-com
plan: 06
type: execute
wave: 1
depends_on: []
files_modified: ["src/store/useWatchlistStore.ts", "src/services/watchlist.ts", "src/components/Sidebar.tsx", "tests/e2e/watchlist-sync.spec.ts"]
autonomous: true
requirements: ["SYNC-01"]
gap_closure: true
must_haves:
  truths:
    - User can select which remote watchlist to sync with from a dropdown in the UI.
  artifacts:
    - path: "src/store/useWatchlistStore.ts"
      provides: "State for all available remote watchlists and selected active watchlist ID."
    - path: "src/components/Sidebar.tsx"
      provides: "Dropdown UI for selecting active watchlist."
  key_links: []
---

<objective>
Update the application to support maintaining and syncing with multiple remote watchlists from Capital.com.

Purpose: The user needs to choose which watchlist to sync with rather than hardcoding to the first one.
Output: Working watchlist selection UI and updated store logic.
</objective>

<execution_context>
@~/.gemini/antigravity/gsd-core/workflows/execute-plan.md
@~/.gemini/antigravity/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-sync-watchlist-with-capital-com/06-UAT.md
@.planning/debug/multiple-watchlists-dropdown.md
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Add getWatchlists endpoint to service</name>
  <files>src/services/watchlist.ts</files>
  <action>
    - Add a `getWatchlists` function that hits `/api/watchlist` (or appropriate capital.com endpoint) to fetch all available watchlists.
    - Return the list of watchlists (id and name) so the store can populate its state.
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <done>Service function is available to fetch all watchlists.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Update store to support multiple watchlists</name>
  <files>src/store/useWatchlistStore.ts</files>
  <action>
    - Add `availableWatchlists` to `WatchlistState` to store an array of `{ id: string, name: string }`.
    - Modify the sync logic to:
      1. Fetch all watchlists via `getWatchlists`.
      2. Set `availableWatchlists` in the store.
      3. If `remoteWatchlistId` is null, set it to the first available watchlist.
      4. Use `remoteWatchlistId` to sync items instead of hardcoding `watchlists[0]`.
    - Add an action `setActiveWatchlist(id: string)` to change the currently synced watchlist. When changed, the local symbols should be cleared and synced from the newly selected remote watchlist.
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <done>Store can track multiple watchlists and switch between them.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Add Dropdown to UI</name>
  <files>src/components/Sidebar.tsx, tests/e2e/watchlist-sync.spec.ts</files>
  <action>
    - Update `Sidebar.tsx` to include a dropdown `<select>` element (or equivalent custom UI component) in the watchlist header section.
    - Populate it using `availableWatchlists` from the store.
    - Bind `onChange` to `setActiveWatchlist`.
    - Update `tests/e2e/watchlist-sync.spec.ts` to mock the endpoints appropriately and select a watchlist before asserting sync results.
  </action>
  <verify>
    <automated>npx playwright test tests/e2e/watchlist-sync.spec.ts</automated>
  </verify>
  <done>UI allows users to select a watchlist and tests pass.</done>
</task>

</tasks>

<success_criteria>
- The user can select from multiple watchlists in the UI dropdown.
- The app correctly syncs symbols with the selected remote watchlist.
</success_criteria>

<output>
Create `.planning/phases/06-sync-watchlist-with-capital-com/06-06-SUMMARY.md` when done
</output>
