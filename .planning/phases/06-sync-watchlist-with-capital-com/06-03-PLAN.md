---
wave: 3
depends_on: ["06-02"]
files_modified: ["src/components/Sidebar.tsx", "src/components/Watchlist.tsx"]
autonomous: true
---

# Phase 06 Plan 3: UI Implementation

## Goal
Add the "Sync" button to the UI with loading, success, and error feedback states for manual synchronization.

## must_haves
- [ ] D-03: Updates while the terminal is open are triggered manually via the "Sync" button without background polling.
- [ ] A functional "Sync" button placed on the right side of the "Watchlist" header in the Sidebar.
- [ ] Appropriate loading/spinner UI states are shown while syncing.
- [ ] Toast notifications for sync success and failure.

## Artifacts this phase produces
- Symbol: `isSyncing` state variable in `Sidebar.tsx`
- Symbol: `<button>` element with sync icon inside the Watchlist header in `Sidebar.tsx`

## Tasks

<task>
  <read_first>
    - src/components/Sidebar.tsx
    - src/store/useWatchlistStore.ts
  </read_first>
  <action>
    In `src/components/Sidebar.tsx`, locate the "Watchlist" panel header containing `<h3>Watchlist</h3>` and the close button.
    Add a "Sync" button using `RefreshCw` from `lucide-react` next to the close button.
    When clicked, the button should trigger a local function that calls `syncWithRemote` from `useWatchlistStore`.
  </action>
  <acceptance_criteria>
    - The sync button is visibly located on the right side of the Watchlist header.
    - Clicking the button successfully invokes `syncWithRemote`.
  </acceptance_criteria>
</task>

<task>
  <read_first>
    - src/components/Sidebar.tsx
    - src/components/ui/use-toast.ts
  </read_first>
  <action>
    Update the "Sync" button click handler in `src/components/Sidebar.tsx` to maintain an `isSyncing` boolean state.
    Set `isSyncing = true` before calling `syncWithRemote`. Disable the button and use a spinning icon (like `Loader2`) while true.
    Wrap the call in a `try...catch` block.
    If it succeeds, use the application's toast notification system (e.g. `toast({ title: 'Success', description: 'Watchlist synced successfully' })`) to show a success message.
    If it throws an error, catch it and show an error toast (`toast({ variant: 'destructive', title: 'Sync Failed', description: error.message })`).
    Finally, set `isSyncing = false` in a `finally` block.
  </action>
  <acceptance_criteria>
    - The button disables and shows a spinning state during the async `syncWithRemote` call.
    - A success toast is displayed when synchronization succeeds.
    - An error toast is displayed when synchronization fails.
    - The button re-enables and stops spinning after completion or failure.
  </acceptance_criteria>
</task>

## Verification
- Rendering `Sidebar.tsx` in the UI shows the new button.
- User click triggers the loading state.
- Successful sync displays a success toast.
- Failed sync displays an error toast.
