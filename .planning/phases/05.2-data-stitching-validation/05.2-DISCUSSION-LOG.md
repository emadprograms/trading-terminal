# Phase 05.2: data-stitching-validation - Discussion Log

> **Audit trail only.** Do not use as input to planning, research, or execution agents.
> Decisions are captured in CONTEXT.md — this log preserves the alternatives considered.

**Date:** 2026-06-19
**Phase:** 05.2-data-stitching-validation
**Areas discussed:** Time synchronization tolerance, Reconnection resilience simulation, Subscription leak tracking, Environment switching state clearance

---

## Time synchronization tolerance

| Option | Description | Selected |
|--------|-------------|----------|
| Exact match | Fail if the first WS tick doesn't perfectly align with the REST timeline | |
| Allow overlapping | Verify the system correctly discards redundant WS ticks without duplicating data | |
| Lenient gap | Allow small delays, but fail if the gap exceeds a specific threshold | ✓ |

**User's choice:** Lenient gap (allow small delays, but fail if the gap exceeds a specific threshold).
**Notes:** None

---

## Reconnection resilience simulation

| Option | Description | Selected |
|--------|-------------|----------|
| Client-side | Use Playwright's network interception to forcefully terminate it on the client side | |
| Server-side | Inject a failure header to the Vercel Proxy to test server-side disconnects | |
| Both | Test both client-side drops and server-side disconnects | ✓ |

**User's choice:** Both (test both client-side drops and server-side disconnects).
**Notes:** None

---

## Subscription leak tracking

| Option | Description | Selected |
|--------|-------------|----------|
| Payload Assertion | Just assert the `unsubscribe` payload was sent correctly when switching assets | |
| Active Count | Build a test utility that tracks active subscriptions over time | |
| Watchlist-Aware Assertion | Verify `unsubscribe` payload using non-watchlist symbols due to reference counting | ✓ |

**User's choice:** The user pointed out that watchlist items stay subscribed by design. 
**Notes:** Agent validated in `ws-manager.ts` that reference counting is used, so testing leaks must be done with assets not in the watchlist.

---

## Environment switching state clearance

| Option | Description | Selected |
|--------|-------------|----------|
| Loading spinner | Verify a loading spinner appears until the Live data is stitched | |
| Instant clear | Verify the chart instantly clears to blank before loading new data | |
| Smooth overwrite | Verify the old data stays visible until smoothly overwritten by the new Live data | ✓ |

**User's choice:** Verify the old data stays visible until smoothly overwritten by the new Live data.
**Notes:** None

---

## the agent's Discretion

None.

## Deferred Ideas

None.
