---
phase: 01.2-secure-proxy-gateway
plan: 03
type: execute
wave: 3
depends_on: [01.2-02]
files_modified: [src/App.tsx, src/components/Sidebar.tsx, src/components/AccountHeader.tsx, src/hooks/useSession.ts, src/App.test.tsx, src/store/useSessionStore.test.ts, src/components/AccountHeader.test.tsx]
autonomous: true
requirements: [AUTH-03, DATA-03]

must_haves:
  truths:
    - "Manual proxyUrl prompt is removed from the UI (App, Sidebar, AccountHeader)"
    - "useSession hook no longer requires proxyUrl for login"
    - "All frontend tests are updated to match new session store schema"
  artifacts:
    - path: "src/components/AccountHeader.tsx"
      provides: "Clean account header without proxy inputs"
    - path: "src/hooks/useSession.ts"
      provides: "Proxy-independent login logic"
---

<objective>
Clean up legacy UI components, hooks, and test suites to reflect the production-only architecture.
- Remove all manual proxy configuration UI elements.
- Simplify authentication hooks.
- Update test suites to match the removed state fields.
</objective>

<execution_context>
@/Users/emadarshadalam/Documents/GitHub/trading-terminal/.gemini/gsd-core/workflows/execute-plan.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/01.2-secure-proxy-gateway/01.2-CONTEXT.md
@.planning/phases/01.2-secure-proxy-gateway/01.2-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Clean up UI and Hooks (App, Sidebar, AccountHeader, useSession)</name>
  <files>src/App.tsx, src/components/Sidebar.tsx, src/components/AccountHeader.tsx, src/hooks/useSession.ts</files>
  <action>
    Modify UI components and hooks to remove `proxyUrl` dependencies:
    1. `src/App.tsx`: Remove `proxyUrl` usage, `onLaunch` handling, and legacy localStorage cleanup.
    2. `src/components/Sidebar.tsx`: Remove "Connect Proxy" button and `onLaunch` prop.
    3. `src/components/AccountHeader.tsx`: Remove `proxyUrl` from store selector, remove proxy input field and its associated state/handlers.
    4. `src/hooks/useSession.ts`: Remove `proxyUrl` check in `loginMutation`. Handshake should proceed directly to `api.post('session', ...)`. Remove `proxyUrl` from store destructuring.
  </action>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <done>All UI and hooks are cleaned of legacy proxy references.</done>
</task>

<task type="auto">
  <name>Task 2: Update Frontend Test Suites for New Store Schema</name>
  <files>src/App.test.tsx, src/store/useSessionStore.test.ts, src/components/AccountHeader.test.tsx</files>
  <action>
    Update all frontend tests to reflect the removal of `proxyUrl`:
    1. `src/store/useSessionStore.test.ts`: Remove tests for `setProxyUrl`, `resetProxyUrl`, and persistence of `proxyUrl`.
    2. `src/App.test.tsx`: Update mocks/assertions to remove `proxyUrl`.
    3. `src/components/AccountHeader.test.tsx`: Remove assertions related to the proxy input field.
    4. Search for any other test files using `proxyUrl` and update them.
  </action>
  <verify>
    <automated>npm test</automated>
  </verify>
  <done>All frontend tests pass with the new store schema.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| UI → State | Clean state ensures no legacy infrastructure data is leaked or prompted. |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01.2-04 | Spoofing | src/components/AccountHeader.tsx | mitigate | Remove proxy input to prevent manual redirection of traffic. |
</threat_model>

<verification>
npm run build && npm test
</verification>

<success_criteria>
- Build completes successfully.
- No 'proxyUrl' references remain in src/components or src/hooks.
- All frontend tests pass.
</success_criteria>

<output>
Create .planning/phases/01.2-secure-proxy-gateway/01.2-03-SUMMARY.md when done
</output>
