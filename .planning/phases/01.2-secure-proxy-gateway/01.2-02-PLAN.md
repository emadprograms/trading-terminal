---
phase: 01.2-secure-proxy-gateway
plan: 02
type: execute
wave: 2
depends_on: [01.2-01]
files_modified: [src/store/useSessionStore.ts, src/api/client.ts]
autonomous: true
requirements: [AUTH-03, DATA-03]

must_haves:
  truths:
    - "localStorage 'proxyUrl' persistence is eliminated"
    - "Frontend strictly uses relative /api paths"
  artifacts:
    - path: "src/store/useSessionStore.ts"
      provides: "Simplified session state"
    - path: "src/api/client.ts"
      provides: "Production-only relative path client"
---

<objective>
Harden the session store and API client by removing legacy infrastructure configuration.
- Eliminate localStorage persistence for infrastructure settings.
- Hard-code the /api prefix as the only entry point.
</objective>

<execution_context>
@/Users/emadarshadalam/Documents/GitHub/trading-terminal/.gemini/gsd-core/workflows/execute-plan.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/01.2-secure-proxy-gateway/01.2-CONTEXT.md
@.planning/phases/01.2-secure-proxy-gateway/01.2-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove proxyUrl from Session Store</name>
  <files>src/store/useSessionStore.ts</files>
  <action>
    Modify `src/store/useSessionStore.ts`:
    1. Remove `proxyUrl` from the state interface and initial state.
    2. Remove `setProxyUrl` and `resetProxyUrl` actions.
    3. Remove `sanitizeUrl` and all `localStorage.getItem('proxyUrl')` / `localStorage.setItem('proxyUrl')` calls.
    4. Remove `DEFAULT_PROXY_URL` constant.
  </action>
  <verify>
    <automated>npm test src/store/useSessionStore.test.ts</automated>
  </verify>
  <done>Session store is free of manual proxy configuration logic.</done>
</task>

<task type="auto">
  <name>Task 2: Harden Relative Path Enforcement in API Client</name>
  <files>src/api/client.ts</files>
  <action>
    Modify `src/api/client.ts`:
    1. Ensure `DEFAULT_PREFIX = '/api'` is the absolute authority.
    2. Remove any fallback logic that might have checked for `useSessionStore.getState().proxyUrl`.
    3. Keep the fatal check for `undefined` paths.
  </action>
  <verify>
    <automated>npm test src/api/client.test.ts</automated>
  </verify>
  <done>API client strictly enforces relative /api paths.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| Browser → Vercel | Relative paths ensure all traffic routes through the Vercel gateway. |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01.2-03 | Information Disclosure | src/store/useSessionStore.ts | mitigate | Stop persisting infrastructure URLs in localStorage. |
</threat_model>

<verification>
npm test src/store/useSessionStore.test.ts src/api/client.test.ts
</verification>

<success_criteria>
- No 'proxyUrl' references remain in src/store or src/api.
- API client strictly uses /api prefix.
</success_criteria>

<output>
Create .planning/phases/01.2-secure-proxy-gateway/01.2-02-SUMMARY.md when done
</output>
