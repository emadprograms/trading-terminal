---
status: testing
phase: 01.2-secure-proxy-gateway
source: [01.2-01-SUMMARY.md]
started: 2026-06-06T17:15:00Z
updated: 2026-06-06T17:25:00Z
---

## Current Test
<!-- OVERWRITE each test - shows where we are -->

number: 2
name: Login & Secret Injection
expected: |
  Enter credentials and log in. Verify that the login request succeeds, confirming the Vercel proxy is correctly injecting Cloudflare Access Service Tokens and forcing HTTP/1.1 (ALPN fix).
awaiting: user response

## Tests

### 1. Cold Start Smoke Test
expected: Clear any local state/cache. Start the application (`npm run dev`). The application should start without errors. Initial load should trigger proxy calls to Vercel (simulated or real) and the UI should load correctly.
result: blocked
blocked_by: server
reason: "User pointed out that deployment is to Vercel. Local testing requires `vercel dev` to simulate serverless functions, or testing against a deployed URL."

### 2. Login & Secret Injection
expected: Enter credentials and log in. Verify that the login request succeeds, confirming the Vercel proxy is correctly injecting Cloudflare Access Service Tokens and forcing HTTP/1.1 (ALPN fix).
result: [pending]

### 3. Account Toggle (DEMO/LIVE)
expected: Toggle between DEMO and LIVE accounts. Observe that requests to `/api/session` or `/api/market` include the correct `x-env` header and the proxy routes to the appropriate backend environment.
result: [pending]

### 4. Market Data Loading
expected: Navigate to a chart or market view. Verify that market data (e.g., prices, symbols) loads correctly, confirming headers like `CST` and `X-SECURITY-TOKEN` are preserved and propagated through the proxy.
result: [pending]

### 5. Error State Propagation
expected: Trigger an intentional error (e.g., invalid credentials or a malformed request). Verify that the proxy correctly propagates the 4xx or 5xx status code and the response body from the backend to the frontend.
result: [pending]

## Summary

total: 5
passed: 0
issues: 0
pending: 5
skipped: 0
blocked: 0

## Gaps

[none yet]
