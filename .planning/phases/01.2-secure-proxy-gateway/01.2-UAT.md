---
status: partial
phase: 01.2-secure-proxy-gateway
source: [01.2-SUMMARY.md, 01.2-01-SUMMARY.md, 01.2-02-SUMMARY.md, 01.2-03-SUMMARY.md]
started: 2026-06-07T12:00:00Z
updated: 2026-06-07T13:00:00Z
---

## Current Test

[testing paused — All tests attempted; blockers remain]

## Tests

### 1. Cold Start Smoke Test
expected: |
  Kill any running server/service. Clear ephemeral state.
  Run `npm run dev` (or `vercel dev` if testing serverless functions locally).
  The application should boot without errors.
  Verify that the initial load succeeds and the terminal shows `[StabilityTrace]` logs indicating proxy initialization or request handling.
result: pass

### 2. Login Flow (Secret Injection)
expected: |
  Attempt to log in.
  Observe the network tab: requests should go to `/api/session` (relative path).
  The proxy should inject Cloudflare Access Service Tokens and force HTTP/1.1.
  The request should succeed, confirming `BACKEND_URL` routing is functional.
result: issue
reported: "It shows disconnected for both demo and live. Login fails because useSession.ts still requires proxyUrl which was removed from the store."
severity: blocker

### 3. StabilityTrace Logging
expected: |
  Perform various actions (login, toggle account, fetch market data).
  Check the server/Vercel logs.
  You should see structured logs prefixed with `[StabilityTrace]` for proxying intent and upstream response statuses.
result: blocked
blocked_by: prior-phase
reason: "Blocked by login failure (Test 2)"

### 4. Removal of Manual Proxy Config
expected: |
  Inspect the UI (Sidebar, AccountHeader, settings).
  Verify there are no longer any input fields or displays for a manual "Proxy URL".
  The application should "just work" using the internal `/api` gateway.
result: issue
reported: "Old UI elements (proxy-input in AccountHeader.tsx) were not removed."
severity: major

### 5. Large Body Handling (ArrayBuffer)
expected: |
  Perform an action that sends a large payload (e.g., placing an order or updating a complex setting).
  Verify the request completes successfully without Vercel 500 "Body is unusable" errors, confirming the `ArrayBuffer` fix is working.
result: blocked
blocked_by: prior-phase
reason: "Blocked by login failure (Test 2)"

## Summary

total: 5
passed: 1
issues: 2
pending: 0
skipped: 0
blocked: 2

## Gaps

- truth: "Login flow succeeds using relative /api paths without requiring a manual proxyUrl in the store."
  status: failed
  reason: "User reported: Login fails because useSession.ts still requires proxyUrl which was removed from the store."
  severity: blocker
  test: 2
- truth: "All manual Proxy URL input fields and displays are removed from the UI (AccountHeader, Sidebar, etc)."
  status: failed
  reason: "User reported: Old UI elements (proxy-input in AccountHeader.tsx) were not removed."
  severity: major
  test: 4
