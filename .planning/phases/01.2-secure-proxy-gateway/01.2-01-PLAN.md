---
phase: 01.2-secure-proxy-gateway
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [api/_utils.ts, api/proxy.test.ts]
autonomous: true
requirements: [AUTH-01, AUTH-02]

must_haves:
  truths:
    - "Vercel proxy targets BACKEND_URL instead of Capital.com directly"
    - "Cloudflare Access Service Tokens are injected in every upstream request"
    - "[StabilityTrace] logs capture request flow and upstream status"
    - "ALPN fix (allowH2: false) is enforced"
  artifacts:
    - path: "api/_utils.ts"
      provides: "Secure proxy routing logic"
  key_links:
    - from: "api/_utils.ts"
      to: "process.env.BACKEND_URL"
      via: "environment variable"
---

<objective>
Transform the Vercel gateway into a secure, production-first bridge to the GHA backend.
- Remove legacy direct-to-Capital routing.
- Implement [StabilityTrace] logging.
- Ensure Cloudflare Service Tokens are properly injected.
- Fix potential 500 errors by hardening body handling.
</objective>

<execution_context>
@/Users/emadarshadalam/Documents/GitHub/trading-terminal/.gemini/gsd-core/workflows/execute-plan.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/01.2-secure-proxy-gateway/01.2-CONTEXT.md
@.planning/phases/01.2-secure-proxy-gateway/01.2-RESEARCH.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Update Proxy Utility with BACKEND_URL and [StabilityTrace]</name>
  <files>api/_utils.ts</files>
  <behavior>
    - Should use process.env.BACKEND_URL as the base for all upstream requests.
    - Should remove getTarget() logic (GHA handles upstream environment targets).
    - Should log [StabilityTrace] for inbound requests (method, path) and upstream responses (status).
    - Should handle request bodies using arrayBuffer to ensure compatibility and fix Vercel 500s.
    - Should maintain ALPN fix (allowH2: false) via sharedAgent.
  </behavior>
  <action>
    Modify `api/_utils.ts`:
    1. Remove `getTarget` function and `process.env.ENV` fallback.
    2. In `proxyRequest`:
       - Validate `process.env.BACKEND_URL` presence; log fatal [StabilityTrace] if missing.
       - Construct upstream `url` by joining `BACKEND_URL` and `path`.
       - Add `console.log` with `[StabilityTrace]` prefix for:
         - "Proxying {method} {path} -> {targetUrl}"
         - "Upstream Response: {statusCode}"
       - Update body handling: use `await req.arrayBuffer()` for POST/PUT/PATCH to avoid streaming issues in Serverless environments.
       - Ensure `CF-Access-Client-Id` and `CF-Access-Client-Secret` are injected if available.
  </action>
  <verify>
    <automated>npm test api/proxy.test.ts</automated>
  </verify>
  <done>Proxy logic targets BACKEND_URL and logs stability traces.</done>
</task>

<task type="auto">
  <name>Task 2: Update Proxy Tests for BACKEND_URL</name>
  <files>api/proxy.test.ts</files>
  <action>
    Update `api/proxy.test.ts`:
    1. Set `process.env.BACKEND_URL = 'https://tunnel.test'` in `beforeEach`.
    2. Update expectations to check for `https://tunnel.test` instead of the legacy Capital.com URLs.
    3. Ensure tests verify header injection (CF Access tokens, x-env).
  </action>
  <verify>
    <automated>npm test api/proxy.test.ts</automated>
  </verify>
  <done>Tests pass with new routing logic.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| Vercel → GHA Tunnel | Cloudflare Access Service Tokens protect this hop. |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01.2-01 | Information Disclosure | api/_utils.ts | mitigate | Strip 'host', 'connection', 'content-length' headers before proxying. |
| T-01.2-02 | Elevation of Privilege | api/_utils.ts | mitigate | Inforce CF-Access tokens for all non-preflight requests. |
</threat_model>

<verification>
npm test api/proxy.test.ts
</verification>

<success_criteria>
- api/_utils.ts uses BACKEND_URL.
- [StabilityTrace] logs are present.
- All proxy tests pass.
</success_criteria>

<output>
Create .planning/phases/01.2-secure-proxy-gateway/01.2-01-SUMMARY.md when done
</output>
