---
phase: "01.2"
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified: ["package.json", "api/_utils.ts", "api/session.ts", "api/market.ts", "api/order.ts", "vercel.json", "src/api/client.ts", "vite.config.ts", "VALIDATION.md"]
autonomous: false
requirements: [AUTH-01, AUTH-02, AUTH-03, DATA-03]
user_setup:
  - service: Vercel
    why: "Inject Cloudflare Access Service Tokens server-side"
    env_vars:
      - name: CF_ACCESS_CLIENT_ID
        source: "Cloudflare Dashboard -> Access -> Service Tokens"
      - name: CF_ACCESS_CLIENT_SECRET
        source: "Cloudflare Dashboard -> Access -> Service Tokens"

must_haves:
  truths:
    - "Browser requests to /api/session, /api/market, and /api/order succeed without CORS preflight failures (OPTIONS returns 200)."
    - "Cloudflare Access Service Tokens are NOT leaked to the browser's Network tab."
    - "ALPN negotiation failures are eliminated via forced HTTP/1.1 on the server-side proxy."
    - "Upstream 4xx/5xx errors are propagated transparently to the frontend (status + body) per REVIEWS.md."
    - "The 'x-env' header is preserved and forwarded to the upstream for environment selection per REVIEWS.md."
    - "VALIDATION.md exists with executable test scenarios for CORS, ALPN, and path routing (Nyquist compliance)."
  artifacts:
    - path: "api/_utils.ts"
      provides: "Shared undici Agent with allowH2: false"
    - path: "api/session.ts"
      provides: "Granular auth proxy handler with error propagation"
    - path: "api/market.ts"
      provides: "Granular market data proxy handler with header preservation"
    - path: "api/order.ts"
      provides: "Granular order execution proxy handler with body streaming"
    - path: "vercel.json"
      provides: "Granular routing configuration"
    - path: "VALIDATION.md"
      provides: "Phase-specific verification criteria"
  key_links:
    - from: "src/api/client.ts"
      to: "/api/session"
      via: "relative path fetch"
      pattern: "fetch\\(['\"]/api/"
    - from: "vercel.json"
      to: "api/session.ts"
      via: "rewrite rule"
      pattern: "\"source\": \"/api/session\""
---

<objective>
Implement a granular, secure serverless proxy gateway on Vercel to resolve the CORS 'Catch-22' and ALPN negotiation failures blocking the frontend-backend handshake.

Purpose: 
1. Move Cloudflare Access token injection to the server-side (Vercel) to satisfy CORS preflight requirements.
2. Force HTTP/1.1 via `undici` to resolve ALPN errors with Cloudflare Tunnels.
3. Incorporate review feedback: transparent error mapping, environment toggle propagation, and header preservation.

Output:
- Shared proxy utility and granular serverless handlers.
- Refactored frontend client and cleanup of local proxying.
- VALIDATION.md for Nyquist compliance.
</objective>

<execution_context>
@C:/Users/Emad/Documents/GitHub/trading-terminal/.gemini/gsd-core/workflows/execute-plan.md
@C:/Users/Emad/Documents/GitHub/trading-terminal/.gemini/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01.2-secure-proxy-gateway/01.2-CONTEXT.md
@.planning/phases/01.2-secure-proxy-gateway/RESEARCH.md
@.planning/phases/01.2-secure-proxy-gateway/01.2-REVIEWS.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 1: Package Legitimacy Check (undici)</name>
  <files>package.json</files>
  <action>
    Verify 'undici' legitimacy on npmjs.com/package/undici before proceeding with installation.
    It is a high-traffic, official Node.js fetch implementation.
    Status: [ASSUMED] (as per Fallback Policy).
  </action>
  <verify>
    Manual check of the package source and reputation.
  </verify>
  <done>
    Package is approved for installation.
  </done>
</task>

<task type="auto" wave="2">
  <name>Task 2: Foundation (undici, utils, validation)</name>
  <files>package.json, api/_utils.ts, VALIDATION.md</files>
  <action>
    1. Install 'undici' to provide low-level ALPN control (per RESEARCH.md).
    2. Create 'api/_utils.ts' exporting a shared 'undici' Agent configured with 'allowH2: false' to force HTTP/1.1 (per D-01 and D-04).
    3. Create 'VALIDATION.md' defining test cases for:
       - CORS: OPTIONS /api/... returns 200 without tokens.
       - ALPN: Target connection must use HTTP/1.1.
       - Routing: /api/session, /api/market, /api/order mapping.
       - Error Mapping: 401/403/500 from upstream correctly propagated.
       - Env Toggle: 'x-env' header presence and forwarding.
  </action>
  <verify>
    <automated>
      npm list undici && ls api/_utils.ts && ls VALIDATION.md
    </automated>
  </verify>
  <done>
    Foundation for the granular proxy is established with validation criteria for review items.
  </done>
</task>

<task type="auto" wave="3">
  <name>Task 3: Granular Handlers Implementation</name>
  <files>api/session.ts, api/market.ts, api/order.ts</files>
  <action>
    Implement three separate Vercel Serverless handlers (per D-01) with feedback from REVIEWS.md:
    1. **Auth & Setup**: Inject 'CF-Access-Client-Id' and 'CF-Access-Client-Secret' from process.env into upstream requests.
    2. **Environment Propagation**: Explicitly preserve and forward the 'x-env' header (Demo/Live toggle) from the frontend (per REVIEWS.md).
    3. **Header Preservation**: Ensure 'Content-Type', 'Accept', 'CST', and 'X-SECURITY-TOKEN' are passed through (per REVIEWS.md).
    4. **Body Handling**: Use undici's 'request' method to stream incoming bodies for POST/PUT requests (critical for 'order' placement).
    5. **Transparent Error Mapping**: Catch upstream responses; if status >= 400, return the exact status code and response body to the frontend instead of generic 500s.
    6. **ALPN Fix**: Use the shared Agent from '_utils.ts' (allowH2: false).
  </action>
  <verify>
    <automated>
      npx tsc api/session.ts api/market.ts api/order.ts --noEmit --esModuleInterop --skipLibCheck
    </automated>
  </verify>
  <done>
    Granular proxy handlers are implemented, addressing ALPN, CORS, and all review feedback.
  </done>
</task>

<task type="auto" wave="3">
  <name>Task 4: Wiring, Frontend Refactor & Cleanup</name>
  <files>vercel.json, src/api/client.ts, vite.config.ts</files>
  <action>
    1. Update 'vercel.json' with explicit rewrites for granular routing (per D-01):
       - /api/session -> api/session.ts
       - /api/market -> api/market.ts
       - /api/order -> api/order.ts
    2. Refactor 'src/api/client.ts' (per D-03):
       - Use relative paths ('/api/session', etc.) exclusively.
       - Ensure 'x-env' header is attached to all outgoing requests.
       - Remove all references to 'CF-Access-Client-Id' and 'CF-Access-Client-Secret'.
    3. Clean up 'vite.config.ts' by removing the 'server.proxy' block (per D-03).
  </action>
  <verify>
    <automated>
      # Verify no CF-Access headers in client code
      ! grep -r "CF-Access-Client-Id" src/api/client.ts && \
      # Verify vercel.json syntax
      node -e "JSON.parse(require('fs').readFileSync('vercel.json'))"
    </automated>
  </verify>
  <done>
    System is wired to the new gateway and frontend is decoupled from infrastructure secrets.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser -> Vercel | Untrusted input; CORS preflight handled here. |
| Vercel -> Cloudflare | Trusted path; Service tokens injected by Vercel. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01.2-01 | Information Disclosure | src/api/client.ts | mitigate | Move Service Token injection to Vercel Serverless tier (per D-02). |
| T-01.2-02 | DoS / Reliability | api/*.ts | mitigate | Force HTTP/1.1 via undici Agent to resolve ALPN negotiation failures (per D-04). |
| T-01.2-03 | Tampering | api/*.ts | mitigate | Use undici streams to forward request bodies bit-for-bit to target. |
| T-01.2-04 | Information Disclosure | api/*.ts | mitigate | Ensure transparent error mapping doesn't leak internal Vercel stack traces; only propagate upstream API responses. |
| T-01.2-SC | Tampering | npm install | mitigate | Package legitimacy check [ASSUMED] for 'undici' (Task 1). |
</threat_model>

<verification>
## Source Audit

SOURCE    | ID      | Feature/Requirement                    | Plan  | Status  | Notes
--------- | ------- | -------------------------------------- | ----- | ------- | ------
GOAL      | —       | Resolve CORS/ALPN via Vercel Proxy     | 01    | COVERED |
REQ       | AUTH-01 | Secure backend proxy                   | 01    | COVERED |
REQ       | AUTH-02 | Dual-token handshake                   | 01    | COVERED |
REQ       | AUTH-03 | Account state sync (Env toggle)        | 01    | COVERED | Propagated via x-env
REQ       | DATA-03 | Market data integration                | 01    | COVERED |
RESEARCH  | —       | undici Agent allowH2: false            | 01    | COVERED |
RESEARCH  | —       | vercel.json routing                    | 01    | COVERED |
CONTEXT   | D-01    | Granular Architecture (session/market/order) | 01 | COVERED |
CONTEXT   | D-02    | Tiered Secret Ownership                | 01    | COVERED |
CONTEXT   | D-03    | Frontend Decoupling (Relative paths)   | 01    | COVERED |
REVIEW    | —       | Propagate Env Toggle (x-env)           | 01    | COVERED | Task 3.2
REVIEW    | —       | Transparent Error Mapping              | 01    | COVERED | Task 3.5
REVIEW    | —       | Preserve Headers (CT/Accept)           | 01    | COVERED | Task 3.3
REVIEW    | —       | Body Handling (POST)                   | 01    | COVERED | Task 3.4

## Overall Phase Checks
1. Deploy to Vercel preview and verify /api/session reachability.
2. Check Network tab for absence of CF-Access tokens.
3. Confirm no ALPN failures in console.
4. Trigger an intentional 401 from upstream and verify frontend receives the 401 (not 500).
</verification>

<success_criteria>
- [ ] /api/session, /api/market, /api/order return 200 OK via the Vercel gateway.
- [ ] No Cloudflare Access tokens appear in the browser's Network tab.
- [ ] ALPN errors (ERR_ALPN_NEGOTIATION_FAILED) are no longer present.
- [ ] Frontend 'x-env' header reaches the upstream server.
- [ ] Upstream errors are propagated with original status and body.
- [ ] VALIDATION.md is present and contains the required test scenarios.
</success_criteria>

<output>
Create .planning/phases/01.2-secure-proxy-gateway/01.2-01-SUMMARY.md when done
</output>
