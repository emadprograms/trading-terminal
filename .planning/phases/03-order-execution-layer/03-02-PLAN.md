---
phase: 03-order-execution-layer
plan: 02
type: execute
wave: 3
depends_on: [03-01]
files_modified: [package.json, src/App.tsx, src/components/TradeControls.tsx, src/components/TradeLog.tsx, src/store/useTradeStore.ts]
autonomous: false
requirements: [EXEC-01, EXEC-02]

must_haves:
  truths:
    - "User can trigger Market and Limit orders from the TradeControls UI."
    - "Trade confirmations appear as interactive toast notifications."
    - "Active positions and working orders are visible in the TradeLog component."
  artifacts:
    - path: "src/components/TradeControls.tsx"
      provides: "Refined trade input interface"
    - path: "src/components/TradeLog.tsx"
      provides: "Visual tracking of trades and orders"
    - path: "src/App.tsx"
      provides: "Global notification provider (sonner)"
---

<objective>
Deliver the user-facing trading interface with real-time feedback and state-aware components.

Purpose: Provide a professional and responsive trading experience.
Output: Updated TradeControls, new TradeLog component, and toast notification integration.
</objective>

<execution_context>
@C:/Users/Emad/Documents/GitHub/trading-terminal/.gemini/gsd-core/workflows/execute-plan.md
@C:/Users/Emad/Documents/GitHub/trading-terminal/.gemini/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/03-order-execution-layer/RESEARCH.md
@src/components/TradeControls.tsx
@src/store/useTradeStore.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install sonner and Setup Toaster</name>
  <files>package.json, src/App.tsx</files>
  <action>
    - Run `npm install sonner`. (Audit: [Approved] per RESEARCH.md).
    - Update `src/App.tsx` to include the `<Toaster />` component from `sonner`.
    - Configure `Toaster` with appropriate theme (dark) and positioning (top-right).
  </action>
  <verify>
    <automated>grep "sonner" package.json && grep "Toaster" src/App.tsx</automated>
  </verify>
  <done>Toast notification infrastructure is ready.</done>
</task>

<task type="auto">
  <name>Task 2: Refactor TradeControls for Execution</name>
  <files>src/components/TradeControls.tsx, src/store/useTradeStore.ts</files>
  <action>
    - Add `placeOrder` async action to `useTradeStore.ts` that:
      1. Calls `tradeApi.placeMarketOrder` or `placeLimitOrder`.
      2. Registers the pending order in state.
      3. Triggers a "Placing order..." toast.
    - Update `src/components/TradeControls.tsx`:
      - Remove `placeOrder` prop (use store action instead).
      - Add inputs for `Level` (for Limit orders).
      - Add a toggle or dropdown to switch between 'MARKET' and 'LIMIT' order types.
      - Connect Buy/Sell buttons to the store's `placeOrder` action.
  </action>
  <verify>
    <automated>grep "useTradeStore" src/components/TradeControls.tsx</automated>
  </verify>
  <done>TradeControls is fully wired to the execution engine.</done>
</task>

<task type="auto">
  <name>Task 3: Build TradeLog Component</name>
  <files>src/components/TradeLog.tsx</files>
  <action>
    Create `src/components/TradeLog.tsx`:
    - Display two sections: "Active Positions" and "Working Orders".
    - Fetch data from `useTradeStore`.
    - For each item, show: Symbol (Epic), Size, Direction, Entry/Level, and Status.
    - Style to match the terminal's dark theme (consistent with Sidebar/Header).
    - Add a "Cancel" button for working orders (wiring can be a stub for now).
    - Register the component in `src/components/Sidebar.tsx` or `src/App.tsx`.
  </action>
  <verify>
    <automated>test -f src/components/TradeLog.tsx</automated>
  </verify>
  <done>TradeLog component provides visual confirmation of account activity.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Full end-to-end trade execution flow with UI and notifications.</what-built>
  <how-to-verify>
    1. Start the terminal and proxy.
    2. Select a ticker (e.g., IX.D.DOW.IFS.IP).
    3. Place a Market BUY order via TradeControls.
    4. Verify a "Placing order..." toast appears.
    5. Verify a "Trade Successful" toast appears after confirmation.
    6. Verify the new position appears in the TradeLog.
    7. Attempt a Limit order and verify it appears in "Working Orders".
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| User -> TradeControls | Input size and levels must be validated to prevent invalid API requests. |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-02 | Denial of Service | TradeControls | mitigate | Implement basic UI throttling (disable button while order is pending) to prevent rapid clicking. |
| T-03-SC | Tampering | npm installs | mitigate | slopcheck + blocking human checkpoint for [ASSUMED]/[SUS] |
		</threat_model>

<verification>
Check that `sonner` is correctly themed and components reflect the global trade state.
</verification>

<success_criteria>
1. Market/Limit orders can be placed from UI.
2. Toast notifications provide immediate feedback.
3. TradeLog displays accurate position/order state.
</success_criteria>

<output>
Create `.planning/phases/03-order-execution-layer/03-02-SUMMARY.md` when done
</output>
