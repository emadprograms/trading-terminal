---
phase: 03-order-execution-layer
plan: 02
type: execute
wave: 3
depends_on: ["03-01"]
files_modified: [src/components/TradeControls.tsx, src/components/TradeLog.tsx, src/store/useTradeStore.ts, src/api/trade.ts, src/store/useTradeStore.risk.test.ts, src/components/TradeControls.test.tsx, src/components/TradeLog.test.tsx]
autonomous: true
requirements: [EXEC-01, EXEC-02, EXEC-03, UI-02]
must_haves:
  truths:
    - "Buy/Sell buttons show a loading indicator and are disabled immediately after a click"
    - "The user can close an active position with a single 'Flatten' click"
    - "Working orders can be cancelled directly from the execution log"
    - "All orders include an automated Stop Loss with the 'Guaranteed' flag set to true"
  artifacts:
    - path: "src/store/useTradeStore.risk.test.ts"
      provides: "Tests for pre-flight risk logic and position management"
    - path: "src/components/TradeControls.tsx"
      provides: "Order entry UI"
    - path: "src/components/TradeLog.tsx"
      provides: "Trade history and management UI"
---

<objective>
Deliver the user-facing execution controls with built-in risk protection, position flattening, and visual feedback loops.

Purpose: Professional trade entry and management experience.
Output: Buy/Sell controls, Flattening/Cancel logic, and pre-flight risk guards.
</objective>

<execution_context>
@/Users/emadarshadalam/Documents/GitHub/trading-terminal/.gemini/gsd-core/workflows/execute-plan.md
@/Users/emadarshadalam/Documents/GitHub/trading-terminal/.gemini/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-order-execution-layer/03-01-SUMMARY.md
@.planning/phases/03-order-execution-layer/UI-SPEC.md
@src/store/useTradeStore.ts
@src/api/trade.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Pre-flight Risk, Flattening & Cancel Logic</name>
  <files>src/store/useTradeStore.ts, src/api/trade.ts, src/store/useTradeStore.risk.test.ts</files>
  <action>
    1. Create `src/store/useTradeStore.risk.test.ts`.
    2. Update `src/api/trade.ts`:
       - Implement `flattenPosition(dealId: string)`: DELETE to `/positions/{dealId}`.
       - Implement `cancelWorkingOrder(id: string)`: DELETE to `/workingorders/{id}`.
    3. Update `src/store/useTradeStore.ts`:
       - Implement Pre-flight SL calculation in `placeOrder`: Calculate `stopLevel` and include `guaranteedStop: true` in the request payload.
       - Implement `flattenPosition` and `cancelWorkingOrder` actions.
       - Implement 0.5% Slippage Safety Cap: Reject/Alert if fill price deviates > 0.5% from target.
  </action>
  <verify>
    <automated>npm run test src/store/useTradeStore.risk.test.ts</automated>
  </verify>
  <done>Flattening, Cancel, and Guaranteed SL logic implemented with test verification.</done>
</task>

<task type="auto">
  <name>Task 2: Implement TradeControls Component</name>
  <files>src/components/TradeControls.tsx, src/components/TradeControls.test.tsx</files>
  <action>
    1. Create `src/components/TradeControls.test.tsx`.
    2. Create `src/components/TradeControls.tsx`:
       - Large BUY and SELL buttons (Colors: #26a69a and #ef5350).
       - Loading state: Disable buttons/show spinner when `useTradeStore` is in `PENDING` state.
       - Size input (Shares).
       - Optimistic feedback: Immediate disable on click.
  </action>
  <verify>
    <automated>npm run test src/components/TradeControls.test.tsx</automated>
  </verify>
  <done>TradeControls UI implemented with optimistic feedback and loading states.</done>
</task>

<task type="auto">
  <name>Task 3: Implement TradeLog & Notifications</name>
  <files>src/components/TradeLog.tsx, src/components/TradeLog.test.tsx</files>
  <action>
    1. Create `src/components/TradeLog.test.tsx`.
    2. Create `src/components/TradeLog.tsx`:
       - List recent orders, working orders, and active positions.
       - "Flatten" button for active positions.
       - "Cancel" button for working orders.
       - Status badges (Pending, Accepted, Rejected).
    3. Integrate `sonner` for toast notifications on all trade outcomes (Success/Failure/Cancel).
  </action>
  <verify>
    <automated>npm run test src/components/TradeLog.test.tsx</automated>
  </verify>
  <done>TradeLog shows history and management controls; User receives clear notifications.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| User -> UI | Size input, Button clicks, Flatten/Cancel clicks |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-04 | Tampering | Size Input | mitigate | Limit maximum allowed trade size in UI and Store |
| T-03-05 | Broken Access Control | Flatten/Cancel | mitigate | Ensure only active positions/orders for the current session can be managed |
</threat_model>

<verification>
Manual visual check of the TradeControls and TradeLog. Verify "Flatten" and "Cancel" buttons trigger the expected API calls.
</verification>

<success_criteria>
1. User can place orders with automated SL.
2. User can flatten positions and cancel working orders.
3. UI reflects state changes and slippage accurately.
</success_criteria>

<output>
Create `.planning/phases/03-order-execution-layer/03-02-SUMMARY.md` when done
</output>
