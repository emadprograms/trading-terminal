---
phase: 03-order-execution-layer
plan: 01
type: execute
wave: 2
depends_on: ["03-00"]
files_modified: [src/store/useTradeStore.ts, src/lib/ws-manager.ts, src/store/useTradeStore.hybrid.test.ts, src/store/useTradeStore.watchdog.test.ts]
autonomous: true
requirements: [EXEC-01, EXEC-02]
must_haves:
  truths:
    - "The order status transitions to 'Accepted' automatically when the WebSocket confirms the trade"
    - "If the internet flickers and a confirmation is missed, the terminal recovers the trade status within 2 seconds"
    - "Positions appear in the active list immediately after they are confirmed"
  artifacts:
    - path: "src/store/useTradeStore.hybrid.test.ts"
      provides: "Tests for WS race conditions and buffering"
    - path: "src/store/useTradeStore.watchdog.test.ts"
      provides: "Tests for REST polling fallback"
---

<objective>
Implement the hybrid synchronization layer that bridges REST submission with real-time confirmation. This ensures high reliability even if WebSocket messages are lost.

Purpose: Robust trade confirmation tracking.
Output: Integrated WS routing, 2-second watchdog, and race-condition buffer.
</objective>

<execution_context>
@/Users/emadarshadalam/Documents/GitHub/trading-terminal/.gemini/gsd-core/workflows/execute-plan.md
@/Users/emadarshadalam/Documents/GitHub/trading-terminal/.gemini/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-order-execution-layer/03-00-SUMMARY.md
@.planning/phases/03-order-execution-layer/03-CONTEXT.md
@.planning/phases/03-order-execution-layer/RESEARCH.md
@src/store/useTradeStore.ts
@src/lib/ws-manager.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Hybrid Confirmation & Race-Condition Buffer</name>
  <files>src/store/useTradeStore.ts, src/lib/ws-manager.ts, src/store/useTradeStore.hybrid.test.ts</files>
  <action>
    1. Create `src/store/useTradeStore.hybrid.test.ts`.
    2. Update `src/store/useTradeStore.ts`:
       - Implement `handleConfirmation(payload: TradeConfirmation)` action.
       - Implement the "Race-Condition Buffer": If a WS confirmation arrives for a `dealReference` that isn't in `pendingOrders` yet (REST response lagging), store it in a `confirmationBuffer`.
       - When `placeOrder` receives its REST response, check the buffer for that `dealReference`.
       - Transition status from `PENDING` to `ACCEPTED` (Position created) or `REJECTED`.
    3. Update `src/lib/ws-manager.ts`:
       - Ensure `handleMessage` identifies `confirms` updates and routes the payload to `useTradeStore.getState().handleConfirmation()`.
  </action>
  <verify>
    <automated>npm run test src/store/useTradeStore.hybrid.test.ts</automated>
  </verify>
  <done>WS confirmations transition pending orders; Buffer handles race conditions with test verification.</done>
</task>

<task type="auto">
  <name>Task 2: Implement 2-Second Watchdog Fallback</name>
  <files>src/store/useTradeStore.ts, src/store/useTradeStore.watchdog.test.ts</files>
  <action>
    1. Create `src/store/useTradeStore.watchdog.test.ts`.
    2. Implement Watchdog logic in `useTradeStore.ts`:
       - In `placeOrder`, after adding to `pendingOrders`, start a `setTimeout` (2000ms).
       - In the timeout: Check if the order is still `PENDING`. If yes, call `tradeApi.getConfirmation(dealReference)`.
       - Clear the watchdog if the WS confirmation arrives first.
  </action>
  <verify>
    <automated>npm run test src/store/useTradeStore.watchdog.test.ts</automated>
  </verify>
  <done>Watchdog successfully polls REST API if WS is missed, with test verification.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| WebSocket -> Store | Confirmation payloads |
| Store -> REST | Fallback polling |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-03 | Repudiation | src/store/useTradeStore.ts | mitigate | Log all transitions (PENDING -> ACCEPTED/REJECTED) to console and store |
</threat_model>

<verification>
Simulate a missing WebSocket message in `useTradeStore.watchdog.test.ts` and verify that the watchdog triggers a REST poll.
</verification>

<success_criteria>
1. WebSocket confirms are routed to the store.
2. Orders transition to ACTIVE/REJECTED correctly.
3. Watchdog triggers at 2s for pending orders.
</success_criteria>

<output>
Create `.planning/phases/03-order-execution-layer/03-01-SUMMARY.md` when done
</output> 
