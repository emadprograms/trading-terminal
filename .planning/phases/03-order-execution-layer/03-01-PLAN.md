---
phase: 03-order-execution-layer
plan: 01
type: execute
wave: 2
depends_on: [03-00]
files_modified: [src/api/trade.ts, src/lib/ws-manager.ts, src/store/useTradeStore.ts]
autonomous: true
requirements: [EXEC-01, EXEC-02]

must_haves:
  truths:
    - "Orders placed via tradeApi return a dealReference and are registered in useTradeStore."
    - "WebSocketManager receives trade confirmations and routes them to useTradeStore."
    - "System handles the async gap between REST order placement and WS confirmation."
  artifacts:
    - path: "src/api/trade.ts"
      provides: "REST methods for order placement"
    - path: "src/lib/ws-manager.ts"
      provides: "Real-time trade confirmation stream"
  key_links:
    - from: "src/api/trade.ts"
      to: "useTradeStore"
      via: "addPendingOrder action"
    - from: "src/lib/ws-manager.ts"
      to: "useTradeStore"
      via: "updateOrderStatus action on message"
---

<objective>
Implement the communication layer with Capital.com for the full trade lifecycle, integrating REST API placement with real-time WebSocket confirmations.

Purpose: Enable live execution and real-time state synchronization.
Output: REST trade client, extended WebSocket manager, and async coordination logic.
</objective>

<execution_context>
@C:/Users/Emad/Documents/GitHub/trading-terminal/.gemini/gsd-core/workflows/execute-plan.md
@C:/Users/Emad/Documents/GitHub/trading-terminal/.gemini/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/03-order-execution-layer/RESEARCH.md
@src/api/client.ts
@src/lib/ws-manager.ts
@src/store/useTradeStore.ts
@src/types/trade.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement tradeApi REST Client</name>
  <files>src/api/trade.ts</files>
  <behavior>
    - `placeMarketOrder(params)`: Calls `POST /positions`, returns `dealReference`.
    - `placeLimitOrder(params)`: Calls `POST /workingorders`, returns `dealReference`.
    - `getConfirmation(dealReference)`: Calls `GET /confirms/{dealReference}` (polling fallback).
  </behavior>
  <action>
    Create `src/api/trade.ts`.
    - Implement REST methods using the shared `api` instance (from `src/api/client.ts`).
    - Methods should handle Capital.com specific payload structures for market and limit orders.
    - Ensure errors (e.g. 400, 401) are caught and thrown with human-readable context.
  </action>
  <verify>
    <automated>npm run test src/api/trade.test.ts</automated>
  </verify>
  <done>REST trade API is functional and verified against MSW mocks.</done>
</task>

<task type="auto">
  <name>Task 2: Extend WebSocketManager for Confirmations</name>
  <files>src/lib/ws-manager.ts</files>
  <action>
    Modify `src/lib/ws-manager.ts`:
    - Add a `subscribeToConfirmations()` method that sends `{ destination: 'confirms.subscribe', ... }`.
    - Call `subscribeToConfirmations()` in `authenticate()` or `onopen`.
    - Update `handleMessage()` to listen for `message.destination === 'confirms'`.
    - When a confirmation is received, call `useTradeStore.getState().handleConfirmation(message.payload)`.
  </action>
  <verify>
    <automated>grep -E "confirms\.subscribe|handleConfirmation" src/lib/ws-manager.ts</automated>
  </verify>
  <done>WebSocket stream now carries trade confirmation events to the global store.</done>
</task>

<task type="auto">
  <name>Task 3: Implement Async Coordination Logic</name>
  <files>src/store/useTradeStore.ts</files>
  <action>
    Enhance `src/store/useTradeStore.ts`:
    - Implement `handleConfirmation(payload)`:
      - Locate the pending order by `dealReference`.
      - Update status to 'ACCEPTED' or 'REJECTED'.
      - If 'ACCEPTED', move to `positions` list.
      - If 'REJECTED', record the `reason`.
    - Add a safeguard: If no confirmation arrives within 5 seconds, attempt a one-time poll via `tradeApi.getConfirmation()`.
  </action>
  <verify>
    <automated>npm run test src/api/trade.test.ts</automated>
  </verify>
  <done>Store correctly coordinates REST placement and async WS confirmations.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| WS -> Store | Confirmation messages from WebSocket must be verified to belong to the current session. |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01 | Information Disclosure | WebSocket Stream | mitigate | Confirmations are only received for the authenticated session; session tokens are mandatory for subscription. |
| T-03-SC | Tampering | npm installs | mitigate | slopcheck + blocking human checkpoint for [ASSUMED]/[SUS] |
</threat_model>

<verification>
Verify that placing a mock order in a test environment correctly triggers the store update flow.
</verification>

<success_criteria>
1. `tradeApi` methods return valid `dealReference`.
2. `wsManager` successfully routes 'confirms' messages to the store.
3. Orders transition from PENDING to ACCEPTED in the store.
</success_criteria>

<output>
Create `.planning/phases/03-order-execution-layer/03-01-SUMMARY.md` when done
</output>
