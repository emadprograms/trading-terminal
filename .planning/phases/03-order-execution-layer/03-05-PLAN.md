---
phase: 03-order-execution-layer
plan: 05
type: execute
wave: 4
depends_on: ["03-02"]
files_modified: [src/api/trade.ts, src/store/useTradeStore.ts, src/components/TradeControls.tsx, src/components/TradeLog.tsx, src/types/trade.ts, src/lib/api-utils.ts, src/store/trade-logic.test.ts, src/lib/api-utils.test.ts]
autonomous: true
requirements: [EXEC-01, EXEC-02, EXEC-03, UI-02]
must_haves:
  truths:
    - "Market orders use stopDistance (Points) and guaranteedStop per D-3.1"
    - "Flatten/Cancel actions use 100ms throttle and try-finally for state safety"
    - "Error messages in toasts are sanitized to remove proxy URLs and internal headers (T-03-07)"
    - "TradeLog displays row-level loading states using closingDealIds set"
  artifacts:
    - path: "src/store/trade-logic.test.ts"
      provides: "Verification for throttling, state safety, and precision risk"
      min_lines: 30
    - path: "src/api/trade.ts"
      provides: "Sanitized error handling and stopDistance support"
      min_lines: 30
    - path: "src/lib/api-utils.ts"
      provides: "Error sanitization utility"
      min_lines: 30
  key_links:
    - from: "src/components/TradeLog.tsx"
      to: "src/store/useTradeStore.ts"
      via: "closingDealIds state subscription"
      pattern: "useTradeStore.*closingDealIds"
    - from: "src/store/useTradeStore.ts"
      to: "src/api/trade.ts"
      via: "tradeApi.closePosition invocation"
      pattern: "tradeApi\\.closePosition"
    - from: "src/api/trade.ts"
      to: "src/lib/api-utils.ts"
      via: "sanitizeErrorMessage wrapper"
      pattern: "sanitizeErrorMessage"
---

<objective>
Revised Execution Hardening (v2): Implement precision risk parameters (stopDistance), throttled orchestration for batch actions, and granular UI feedback with robust state safety and verified error sanitization.

Purpose: Professional-grade reliability, risk precision, and secure error disclosure.
Output: Hardened trade store with try-finally safety, row-level loading in TradeLog, and verified sanitized toast notifications.
</objective>

<execution_context>
@/Users/emadarshadalam/Documents/GitHub/trading-terminal/.gemini/gsd-core/workflows/execute-plan.md
@/Users/emadarshadalam/Documents/GitHub/trading-terminal/.gemini/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-order-execution-layer/03-02-SUMMARY.md
@.planning/phases/03-order-execution-layer/RESEARCH.md
@.planning/phases/03-order-execution-layer/REVIEWS.md
@src/store/useTradeStore.ts
@src/api/trade.ts
@src/lib/api-utils.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Hardened Trade Store & Throttled Orchestration</name>
  <files>src/store/useTradeStore.ts, src/store/trade-logic.test.ts, src/types/trade.ts</files>
  <behavior>
    - Test 1: `flattenAll` iterates through active positions with 100ms sleep between calls.
    - Test 2: `flattenAll` uses `try-finally` to ensure `isExecuting` and `closingDealIds` are cleared even if a DELETE fails.
    - Test 3: `closingDealIds` (Set) correctly tracks individual deal IDs during the closing process.
    - Test 4: `cancelAllWorkingOrders` implements the same throttling and safety logic for working orders.
  </behavior>
  <action>
    1. Update `src/types/trade.ts`:
       - Add `stopDistance: number` and `guaranteedStop: boolean` to `Order` and `Position` types.
       - Ensure API parameter interfaces include these fields.
    2. Update `src/store/useTradeStore.ts`:
       - Add `closingDealIds: Set<string>` to the state.
       - Implement `flattenAll()`:
         - Set `isExecuting = true`.
         - Iterate through positions: add to `closingDealIds`, call `tradeApi.closePosition`, wait 100ms, then remove from `closingDealIds`.
         - Wrap the loop in `try...finally` to reset `isExecuting = false` and clear `closingDealIds`.
       - Implement `cancelAllWorkingOrders()` using the same pattern.
       - Refactor `placeOrder` to use `try...finally` for its `isExecuting` flag.
  </action>
  <verify>
    <automated>npm run test src/store/trade-logic.test.ts</automated>
  </verify>
  <done>Throttled orchestration and state safety logic implemented in the trade store.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Precision API & Error Sanitization</name>
  <files>src/api/trade.ts, src/lib/api-utils.ts, src/lib/api-utils.test.ts</files>
  <behavior>
    - Test 1: `sanitizeErrorMessage` removes strings matching `https://*.vercel.app`.
    - Test 2: `sanitizeErrorMessage` removes `x-security-token` and `cst` headers from strings.
    - Test 3: API calls wrap errors with `sanitizeErrorMessage`.
  </behavior>
  <action>
    1. Create `src/lib/api-utils.test.ts` with tests for sanitization logic.
    2. Update `src/lib/api-utils.ts`:
       - Implement `sanitizeErrorMessage(error: any): string`.
       - It must strip proxy URLs (e.g., `https://*.vercel.app`) and internal headers (e.g., `x-security-token`, `cst`) from error strings or error objects.
    3. Update `src/api/trade.ts`:
       - Update `placeMarketOrder` and `placeLimitOrder` to accept and send `stopDistance` and `guaranteedStop`.
       - Wrap API calls in `try...catch` and apply `sanitizeErrorMessage` before re-throwing or returning error state.
       - Specifically handle `400/403` for `guaranteedStop` by providing a clear, sanitized user message.
  </action>
  <verify>
    <automated>npm run test src/api/trade.test.ts src/lib/api-utils.test.ts</automated>
  </verify>
  <done>API client supports precision parameters and provides sanitized error messages verified by tests.</done>
</task>

<task type="auto">
  <name>Task 3: Precision UI & Granular Feedback</name>
  <files>src/components/TradeLog.tsx, src/components/TradeControls.tsx</files>
  <action>
    1. Update `src/components/TradeControls.tsx`:
       - Add `Stop Distance (Points)` numeric input.
       - Add `Guaranteed Stop` checkbox (default checked).
       - Ensure `placeOrder` passes these values to the store.
    2. Update `src/components/TradeLog.tsx`:
       - In both Positions and Working Orders tables, check `closingDealIds.has(dealId)`.
       - If true, display a spinner/loading icon on the "Close" or "Cancel" button for that row.
       - Disable the button while the specific deal is in flight.
    3. Update Toast notifications (using `sonner`):
       - Ensure `toast.error` uses the sanitized message from the store/API.
  </action>
  <verify>
    <automated>npm run test src/components/TradeLog.test.tsx</automated>
  </verify>
  <done>UI provides granular feedback for batch actions and supports precision risk inputs.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| Browser -> API | stopDistance and size parameters must be validated by Proxy/API |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-06 | Denial of Service | Flatten Action | mitigate | Enforce 100ms throttle between API calls to avoid 429 Rate Limiting |
| T-03-07 | Information Disclosure | Trade Errors | mitigate | Sanitize error messages in `lib/api-utils.ts` to strip proxy URLs and internal headers before display |
</threat_model>

<verification>
1. Place a Market Order with a 50 point stopDistance; verify payload in Network tab.
2. Trigger "Flatten All" with 3+ positions; verify 100ms gaps between DELETE calls.
3. Verify that individual row spinners appear in TradeLog during the "Flatten All" process.
4. Simulate a 403 error for Guaranteed Stop; verify toast message is sanitized and helpful.
</verification>

<success_criteria>
1. All market orders include `stopDistance` and `guaranteedStop` per research requirements.
2. "Flatten" and "Cancel All" operations are throttled (100ms) and state-safe (try-finally).
3. `TradeLog` shows row-level loading indicators during execution.
4. Error toasts do not leak proxy URLs or internal headers.
</success_criteria>

<output>
Create `.planning/phases/03-order-execution-layer/03-05-SUMMARY.md` when done
</output>
