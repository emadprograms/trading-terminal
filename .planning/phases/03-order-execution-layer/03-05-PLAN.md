---
phase: 03-order-execution-layer
plan: 05
type: execute
wave: 1
depends_on: []
files_modified: 
  - src/store/useTradeStore.ts
  - src/lib/api-utils.ts
autonomous: false
requirements: [EXEC-01, EXEC-02, EXEC-03]
user_setup: []

must_haves:
  truths:
    - "Trade buttons unlock immediately after any API call (success or failure)."
    - "Market order payloads do not contain 'guaranteedStop: false'."
    - "Limit/Stop orders route to /v1/workingorders and Market orders route to /v1/positions."
    - "All sanitized error tokens in UI are unified to '[INTERNAL_URL]'."
  artifacts:
    - path: "src/store/useTradeStore.ts"
      provides: "Corrected isExecuting lifecycle and payload logic"
    - path: "src/lib/api-utils.ts"
      provides: "Unified sanitization tokens"
  key_links:
    - from: "src/store/useTradeStore.ts"
      to: "tradeApi.placeMarketOrder"
      via: "Payload filtering"
    - from: "src/store/useTradeStore.ts"
      to: "tradeApi.placeLimitOrder"
      via: "Routing logic"
---

<objective>
Perform a surgical closure of 4 critical gaps identified in 03-UAT.md. 

**CRITICAL CONSTRAINT:** To avoid the total connectivity loss seen in the previous attempt, this plan implements changes atomically. 
**WARNING:** Do NOT modify connection strings, headers, base `ky` client configuration, or any logic in `src/api/client.ts`.
</objective>

<execution_context>
@C:/Users/Emad/Documents/GitHub/trading-terminal/.gemini/gsd-core/workflows/execute-plan.md
@C:/Users/Emad/Documents/GitHub/trading-terminal/.gemini/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-order-execution-layer/03-UAT.md
@.planning/debug/phase-03-gap-closure-failure.md
@src/store/useTradeStore.ts
@src/lib/api-utils.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix UI Button Locking (Surgical)</name>
  <files>src/store/useTradeStore.ts</files>
  <action>
    In `placeOrder`, move the `set({ isExecuting: false })` call from the `catch` block to the `finally` block. 
    
    Current:
    ```typescript
    } catch (error) {
      set({ isExecuting: false });
      throw error;
    } finally { ... }
    ```
    
    Target:
    ```typescript
    } catch (error) {
      throw error;
    } finally {
      set({ isExecuting: false });
    }
    ```
    
    Warning: Do NOT touch any API client config or headers.
  </action>
  <verify>
    <automated>
      1. Trigger a failed order (e.g., use an invalid Epic).
      2. Confirm in DevTools/Console that the API returns an error.
      3. Verify that Buy/Sell buttons are immediately clickable again.
    </automated>
  </verify>
  <done>Buttons unlock regardless of API outcome.</done>
</task>

<task type="auto">
  <name>Task 2: Fix Market Order 400 Error (Payload Tuning)</name>
  <files>src/store/useTradeStore.ts</files>
  <action>
    Modify `placeOrder` payload construction to ensure `guaranteedStop` is only included if it is `true`.
    
    Replace:
    ```typescript
    const finalParams = {
      ...apiParams,
      guaranteedStop: params.guaranteedStop ?? false,
    };
    ```
    With:
    ```typescript
    const finalParams = { ...apiParams };
    if (params.guaranteedStop) {
      finalParams.guaranteedStop = true;
    }
    ```
    
    Warning: Do NOT touch any API client config or headers.
  </action>
  <verify>
    <automated>
      1. Place a Market order with 'Guaranteed Stop' UNCHECKED.
      2. Inspect the Network request payload in DevTools.
      3. Verify that the key 'guaranteedStop' is COMPLETELY ABSENT from the JSON body.
    </automated>
  </verify>
  <done>Payload contains no explicit 'false' for guaranteedStop.</done>
</task>

<task type="auto">
  <name>Task 3: Verify and Harden Order Routing</name>
  <files>src/store/useTradeStore.ts</files>
  <action>
    Ensure routing is explicit. Add a temporary `console.log` to the routing logic to verify behavior in the browser.
    
    In `placeOrder`, ensure the logic remains:
    - `LIMIT` or `STOP` -> `tradeApi.placeLimitOrder`
    - Otherwise -> `tradeApi.placeMarketOrder`
    
    Add log: `console.log("[Surgical-Verify] Routing order type ${orderType} to ${orderType === 'LIMIT' || orderType === 'STOP' ? 'workingorders' : 'positions'}`);`
    
    Warning: Do NOT modify `tradeApi.ts` or `client.ts`. Only modify the store's call site.
  </action>
  <verify>
    <automated>
      1. Place a Limit order -> Verify console log shows 'workingorders' and network request hits `/v1/workingorders`.
      2. Place a Market order -> Verify console log shows 'positions' and network request hits `/v1/positions`.
    </automated>
  </verify>
  <done>Routing is verified and behaves as expected.</done>
</task>

<task type="auto">
  <name>Task 4: Unify Error Sanitization Tokens</name>
  <files>src/lib/api-utils.ts</files>
  <action>
    Unify all sensitive data replacement tokens to `[INTERNAL_URL]`.
    
    In `sanitizeErrorMessage`, replace the token used for sensitive headers (currently `[REDACTED]`) with `[INTERNAL_URL]`.
    
    Target:
    ```typescript
    return `${parts[0].trim()}: [INTERNAL_URL]`;
    ```
    
    Warning: Ensure this only modifies the string return value for UI consumption, NOT any request/response headers.
  </action>
  <verify>
    <automated>
      1. Trigger an API error that includes a sensitive header (or mock one).
      2. Verify the resulting UI toast/message contains `[INTERNAL_URL]` and NO instances of `[REDACTED]`.
    </automated>
  </verify>
  <done>All sanitization tokens are unified to [INTERNAL_URL].</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| Client -> Proxy | Untrusted UI input reaches Proxy |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-05-01 | Information Disclosure | sanitizeErrorMessage | mitigate | Unify tokens to [INTERNAL_URL] to hide proxy/internal structure |
| T-03-05-SC | Tampering | npm installs | mitigate | No new packages introduced in this gap closure |
</threat_model>

<verification>
All 4 tasks must be verified individually using DevTools Network tab and Console logs before completing the plan. 
Any total connectivity loss must trigger an immediate revert to commit `f502491`.
</verification>

<success_criteria>
1. Order buttons unlock on failure (Verified via manual trigger).
2. Market order request body contains no `guaranteedStop: false` (Verified via Network tab).
3. Limit orders hit `/v1/workingorders`, Market orders hit `/v1/positions` (Verified via Network tab).
4. No `[REDACTED]` tokens appear in UI errors (Verified via UI toast).
</success_criteria>

<output>
Create `.planning/phases/03-order-execution-layer/03-05-SUMMARY.md` when done
</output>
