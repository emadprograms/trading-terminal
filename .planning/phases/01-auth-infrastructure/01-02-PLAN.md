---
phase: 01-auth-infrastructure
plan: 02
type: execute
wave: 2
depends_on: [01-01]
files_modified: [src/store/useSessionStore.ts, src/api/client.ts, src/hooks/useSession.ts]
autonomous: true
requirements: [AUTH-01]

must_haves:
  truths:
    - "CST and X-SECURITY-TOKEN are captured from the login response"
    - "Tokens are stored in-memory (Zustand) and never persisted to disk"
    - "Subsequent API calls automatically include the session tokens"
  artifacts:
    - path: "src/store/useSessionStore.ts"
      provides: "In-memory token management"
    - path: "src/api/client.ts"
      provides: "Configured Ky client"
    - path: "src/hooks/useSession.ts"
      provides: "Auth handshake logic"
  key_links:
    - from: "src/hooks/useSession.ts"
      to: "src/api/client.ts"
      via: "API calls"
    - from: "src/api/client.ts"
      to: "src/store/useSessionStore.ts"
      via: "State access in interceptors"
---

<objective>
Implement the authentication handshake and session management logic on the frontend. This ensures the terminal can securely acquire and use Capital.com session tokens.

Purpose: Automate the dual-token handshake and handle session lifecycle.
Output: Working auth hook and API client.
</objective>

<execution_context>
@C:/Users/Emad/Documents/GitHub/trading-terminal/.gemini/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-auth-infrastructure/01-CONTEXT.md
@.planning/phases/01-auth-infrastructure/01-RESEARCH.md
@.planning/phases/01-auth-infrastructure/01-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Session Store</name>
  <files>src/store/useSessionStore.ts</files>
  <action>
    - Install `zustand`.
    - Implement `useSessionStore` to hold:
      1. `cst`: string | null
      2. `securityToken`: string | null
      3. `proxyUrl`: string | null (to be set from discovery)
      4. `environment`: 'DEMO' | 'LIVE'
      5. `isAuthenticated`: boolean
    - Ensure tokens are NOT persisted (no `persist` middleware).
    - per D-05, D-06.
  </action>
  <verify>
    <automated>Unit test for Zustand store state transitions.</automated>
  </verify>
  <done>Session store manages tokens and environment state in-memory.</done>
</task>

<task type="auto">
  <name>Task 2: Configure Ky Client</name>
  <files>src/api/client.ts</files>
  <action>
    - Install `ky`.
    - Create a `ky` instance with:
      1. `prefixUrl` pointing to `proxyUrl` from the store.
      2. `beforeRequest` hook to inject `CST` and `X-SECURITY-TOKEN` from the store.
      3. `afterResponse` hook to capture `CST` and `X-SECURITY-TOKEN` from headers during the `/session` call.
    - per Research Pattern 1.
  </action>
  <verify>
    <automated>Mocked API test verifying header injection and capture.</automated>
  </verify>
  <done>Ky client automatically handles session header orchestration.</done>
</task>

<task type="auto">
  <name>Task 3: Implement useSession Hook</name>
  <files>src/hooks/useSession.ts</files>
  <action>
    - Create `useSession` hook using `@tanstack/react-query`.
    - Implement:
      1. `login()` function that calls `POST /session` via the proxy.
      2. `logout()` function that clears the store.
      3. `ping()` background heartbeat to keep the session alive (per Pitfall 2).
      4. Auto-login on mount if `proxyUrl` is available.
  </action>
  <verify>
    <automated>Integration test for the handshake flow (mocked proxy).</automated>
  </verify>
  <done>Session hook orchestrates the full auth lifecycle and keeps it alive.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| Zustand -> Memory | Private app state |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | Information Disclosure | LocalStorage | mitigate | Strictly enforce no persistence for session tokens (D-05). |
| T-02-SC | Tampering | npm installs | mitigate | slopcheck + blocking human checkpoint for ky, zustand. |
</threat_model>

<verification>
- Store state updates correctly on login.
- Ky requests include the expected headers.
- Session remains active through heartbeat pings.
</verification>

<success_criteria>
- Dual-token handshake is fully automated.
- Session tokens are securely managed in RAM.
</success_criteria>

<output>
Create `.planning/phases/01-auth-infrastructure/01-02-SUMMARY.md` when done
</output>
