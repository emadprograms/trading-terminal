---
phase: 01-auth-infrastructure
plan: 03
type: execute
wave: 3
depends_on: [01-02]
files_modified: [src/components/AccountHeader.tsx, src/components/EnvToggle.tsx, src/App.tsx, src/components/Sidebar.tsx, package.json]
autonomous: false
requirements: [AUTH-03, DATA-03]
must_haves:
  truths:
    - "User can toggle between Demo and Live environments"
    - "Account equity and margin are displayed in the header"
    - "Visual confirmation of 'Online' status when connected"
  artifacts:
    - path: "src/components/EnvToggle.tsx"
      provides: "Environment switching UI"
    - path: "src/components/AccountHeader.tsx"
      provides: "Real-time account state display"
---

<objective>
Build the UI components for account management and state synchronization. This provides the user with visibility and control over their trading environment and account health.

Purpose: Provide visual feedback for auth and account state.
Output: Integrated header with toggle and account metrics.
</objective>

<execution_context>
@C:/Users/Emad/Documents/GitHub/trading-terminal/.gemini/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-auth-infrastructure/01-CONTEXT.md
@.planning/phases/01-auth-infrastructure/01-UI-SPEC.md
@.planning/phases/01-auth-infrastructure/01-02-SUMMARY.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <what-built>Package legitimacy audit check</what-built>
  <how-to-verify>
    Verify that `lucide-react` is a reputable package.
    1. Visit npmjs.com/package/lucide-react
    Confirm it has high downloads and no major security advisories.
  </how-to-verify>
  <resume-signal>approved</resume-signal>
</task>

<task type="auto" tdd="true">
  <name>Task 1: Build Environment Toggle</name>
  <files>src/components/EnvToggle.tsx</files>
  <behavior>
    - Component renders two buttons for DEMO and LIVE
    - Active environment has a highlighted background
    - Clicking a button triggers useSession.login with the corresponding environment
    - Shows a loading state during handshake (per D-07)
  </behavior>
  <action>
    - Install `lucide-react`.
    - Create `EnvToggle` component per UI-SPEC.
    - Features:
      1. Pill-style switch between "DEMO" and "LIVE".
      2. Triggers `useSession.login()` with the selected environment.
      3. Shows a loading spinner during the handshake.
      4. uses `lucide-react` icons (e.g., `Globe` for Live, `FlaskConical` for Demo per Research supporting stack).
  </action>
  <verify>
    <automated>npm test src/components/EnvToggle.test.tsx</automated>
  </verify>
  <done>Environment toggle allows seamless switching with visual feedback.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement Account Header</name>
  <files>src/components/AccountHeader.tsx</files>
  <behavior>
    - Fetches account data from /accounts
    - Displays Equity, Margin, and Available funds
    - Formats numbers as currency
    - Displays a green dot when session is active
  </behavior>
  <action>
    - Create `AccountHeader` component.
    - Displays:
      1. Equity (formatted with currency).
      2. Margin / Margin Level.
      3. Unused Margin (Available funds).
      4. Connection status indicator (Teal dot when online).
    - Fetches data from `/accounts` via the proxy using `useQuery` (per DATA-03).
  </action>
  <verify>
    <automated>npm test src/components/AccountHeader.test.tsx</automated>
  </verify>
  <done>Account metrics are visible and update in real-time (polling).</done>
</task>

<task type="auto">
  <name>Task 3: Terminal Integration</name>
  <files>src/App.tsx, src/components/Sidebar.tsx</files>
  <action>
    - Integrate `AccountHeader` and `EnvToggle` into the top bar of `App.tsx`.
    - Ensure the "Launch Terminal" splash screen (per UI-SPEC) is shown if no `proxyUrl` or session exists.
    - Wire the global loading overlay during environment resets.
  </action>
  <verify>
    <checkpoint:human-verify gate="blocking">
      <what-built>Full UI integration of Auth and Account Sync.</ what-built>
      <how-to-verify>
        1. Open the terminal.
        2. Verify "Launch Terminal" state.
        3. Trigger GHA and provide Proxy URL in STATE.md.
        4. Verify automatic login and account data display.
        5. Toggle between Demo/Live and verify state reset.
      </how-to-verify>
      <resume-signal>approved</resume-signal>
    </checkpoint:human-verify>
  </verify>
  <done>Terminal UI is fully synchronized with the authenticated backend state.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| UI -> API | Requesting sensitive account data |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-04 | Information Disclosure | UI Components | mitigate | Sanitize account numbers/IDs in the UI display if necessary. |
</threat_model>

<verification>
- Toggle correctly resets the session.
- Account data matches the environment (Demo vs Live).
- Header layout matches the UI specification.
</verification>

<success_criteria>
- User has full visibility into account state.
- Environment switching is robust and visual.
</success_criteria>

<output>
Create `.planning/phases/01-auth-infrastructure/01-03-SUMMARY.md` when done
</output>
