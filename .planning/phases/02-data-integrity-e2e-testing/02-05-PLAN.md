---
phase: 02-data-integrity-e2e-testing
plan: 05
subsystem: testing
status: draft
requires: ["02-04"]
provides: ["Passing unit test suite without regressions"]
affects: ["src/components/ChartCanvas.tsx", "src/hooks/useChartData.ts", "src/lib/sync-coordinator.ts", "tests/performance/render.perf.test.tsx", "src/components/EnvToggle.test.tsx"]
---

# Phase 02 Plan 05: Unit Test Gap Closure

## Objective
Fix the remaining 14 unit test failures that were discovered during phase verification to achieve a fully green test suite.

## Context
While the Playwright E2E suite now passes successfully after fixing `critical-path.spec.ts` to use `domcontentloaded`, the unit test suite still has 14 failures:
1. `EnvToggle.test.tsx` (`mockLogin` not called)
2. `ChartCanvas.tsx` (`markers.filter` is undefined in `selection.test.tsx` and `sync-lag.test.tsx`)
3. `render.perf.test.tsx` (Render count unexpected increase)
4. `sync-coordinator.ts` (`DataStitchingError` incorrectly thrown when returning history directly)
5. `useChartData.ts` (TypeError: `useWorkspaceStore.getState` is not a function)

## Tasks

### 1. Fix `useWorkspaceStore` in `useChartData.ts`
- **Action**: Update `useChartData.ts` to correctly import and use `useWorkspaceStore`.
- **Verify**: `useChartData.test.tsx` passes.

### 2. Fix `markers` undefined error in `ChartCanvas.tsx`
- **Action**: In `src/components/ChartCanvas.tsx`, ensure `markers` is defaulted to an empty array (e.g., `markers = []`) if it is undefined.
- **Verify**: `selection.test.tsx` and `sync-lag.test.tsx` pass.

### 3. Fix SyncCoordinator Gap Detection
- **Action**: In `src/lib/sync-coordinator.ts`, ensure that if there are no buffered ticks, it correctly returns history directly without throwing a `DataStitchingError`.
- **Verify**: `sync-coordinator.test.ts` passes.

### 4. Fix React Render Cycle and EnvToggle tests
- **Action**: 
  - Investigate `tests/performance/render.perf.test.tsx` to prevent unnecessary re-renders.
  - In `src/components/EnvToggle.test.tsx`, ensure `mockLogin` is correctly mocked and triggered by `fireEvent.click(liveBtn)`.
- **Verify**: Both `render.perf.test.tsx` and `EnvToggle.test.tsx` pass.

## Verification
- Run `npm run test` and confirm 0 failing tests.
