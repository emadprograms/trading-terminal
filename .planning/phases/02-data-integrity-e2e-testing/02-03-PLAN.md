---
phase: 02-data-integrity-e2e-testing
plan: 03
type: execute
wave: 3
depends_on: ["02-02"]
files_modified: ["src/hooks/useTradeManager.ts", "src/hooks/useChartLifecycle.ts", "src/components/ChartHeader.tsx", "src/utils/resampling.ts"]
autonomous: true
requirements: [TEST-02]

must_haves:
  truths:
    - "Unit test suite passes all tests without regressions"
  artifacts:
    - path: "tests/hooks/useTradeManager.test.ts"
      provides: "Passing tests for trade manager"
  key_links: []
---

<objective>
Fix the 35 failing unit tests that were broken by recent changes, focusing on React lifecycle errors and missing contexts.

Purpose: To restore the unit test suite to a passing state, a prerequisite for phase validation.
Output: Fixed source files ensuring all unit tests pass.
</objective>

<execution_context>
@.agent/gsd-core/workflows/execute-plan.md
@.agent/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-data-integrity-e2e-testing/02-VERIFICATION.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Fix useTradeManager and useChartLifecycle</name>
  <files>src/hooks/useTradeManager.ts, src/hooks/useChartLifecycle.ts</files>
  <action>Fix TypeError where `tradePluginRef.current.setItems` and `initPriceSeriesRef.current.applyOptions` are not functions. Ensure refs are correctly initialized and populated before calling methods on them. Check if the testing mocks for these third-party library objects need to be updated to support these methods.</action>
  <verify>
    <automated>npm run test -- --testPathPattern=useTradeManager</automated>
  </verify>
  <done>useTradeManager and useChartLifecycle tests pass.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Fix ChartHeader context issues</name>
  <files>src/components/ChartHeader.tsx</files>
  <action>Fix TypeError where `usePriceStore` is not a function when rendered in tests. Ensure the component imports `usePriceStore` correctly and that the test environment mocks it correctly if necessary.</action>
  <verify>
    <automated>npm run test -- --testPathPattern=ChartHeader</automated>
  </verify>
  <done>ChartHeader tests pass.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Fix resampling and shading test logic</name>
  <files>src/utils/resampling.ts, tests/performance/shading.perf.test.ts</files>
  <action>Fix assertion errors in resampling (12:00 vs 00:00 mismatch) and ensure `getSessionType` is correctly imported and mocked if needed in shading.perf.test.ts.</action>
  <verify>
    <automated>npm run test</automated>
  </verify>
  <done>All unit tests pass successfully.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Internal Components | Unit test logic |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-SC | Tampering | npm packages | mitigate | slopcheck + blocking human checkpoint for [ASSUMED]/[SUS] |
</threat_model>

<verification>
Run `npm run test -- --run` to ensure 100% pass rate.
</verification>

<success_criteria>
No unit tests fail.
</success_criteria>

<output>
Create `.planning/phases/02-data-integrity-e2e-testing/02-03-SUMMARY.md` when done
</output>
