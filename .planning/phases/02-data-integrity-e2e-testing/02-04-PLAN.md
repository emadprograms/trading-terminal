---
phase: 02-data-integrity-e2e-testing
plan: 04
type: execute
wave: 3
depends_on: ["02-02", "02-03"]
files_modified: ["tests/regression/sync/propagation.spec.ts", "tests/regression/viewport/visual.spec.ts"]
autonomous: true
requirements: [TEST-02]

must_haves:
  truths:
    - "Playwright E2E regression suite passes without failures"
  artifacts:
    - path: "tests/regression/sync/propagation.spec.ts"
      provides: "Passing propagation tests"
  key_links: []
---

<objective>
Fix the 3 failing Playwright tests in `propagation.spec.ts` and `visual.spec.ts`.

Purpose: To restore the Playwright test suite to a passing state for phase validation.
Output: Fixed Playwright test scripts.
</objective>

<execution_context>
@.agent/gsd-core/workflows/execute-plan.md
@.agent/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-data-integrity-e2e-testing/02-VERIFICATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix propagation.spec.ts locator errors</name>
  <files>tests/regression/sync/propagation.spec.ts</files>
  <action>Fix the locators `div[class*="ChartHeader"] span:has-text("ETH")` and `SOL` which are currently resolving to 0 elements instead of the expected 2. Check if the UI structure changed or if there's a timing issue.</action>
  <verify>
    <automated>npx playwright test tests/regression/sync/propagation.spec.ts</automated>
  </verify>
  <done>Propagation tests pass.</done>
</task>

<task type="auto">
  <name>Task 2: Fix visual.spec.ts timeout</name>
  <files>tests/regression/viewport/visual.spec.ts</files>
  <action>Fix the `locator('canvas').first().hover()` timeout. Ensure the canvas element exists before hovering or address any loading state that blocks the canvas.</action>
  <verify>
    <automated>npx playwright test tests/regression/viewport/visual.spec.ts</automated>
  </verify>
  <done>Visual stability tests pass.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Client→API | Playwright E2E interactions |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-SC | Tampering | npm packages | mitigate | slopcheck + blocking human checkpoint for [ASSUMED]/[SUS] |
</threat_model>

<verification>
Run `npx playwright test` to ensure 100% pass rate.
</verification>

<success_criteria>
No Playwright tests fail.
</success_criteria>

<output>
Create `.planning/phases/02-data-integrity-e2e-testing/02-04-SUMMARY.md` when done
</output>
