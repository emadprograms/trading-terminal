---
phase: 01-chart-alerts-e2e-benchmark-tdd
plan: 1
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/e2e/chart-alerts.spec.ts
autonomous: true
requirements:
  - TEST-01
estimate:
  tokens: 15000
  raw_tokens: 10000
  tasks: 1
  confidence: high
must_haves:
  truths:
    - The E2E test accurately simulates the required chart interactions (hover and click on Y-axis).
    - The E2E test scripts the complete flow, including form submission and verifying the alert is created/visible.
    - The test fails because the features are not yet implemented.
  artifacts:
    - tests/e2e/chart-alerts.spec.ts
  key_links:
    - Playwright test runner can execute the newly created test file.
---

<objective>
Build the strict Playwright E2E test that validates the chart interaction flow before any implementation.

Purpose: Establish a failing benchmark test that defines the expected behavior of the chart alerts feature before development begins.
Output: A new failing E2E test file `tests/e2e/chart-alerts.spec.ts`.
</objective>

<execution_context>
@.agents/gsd-core/workflows/execute-plan.md
@.agents/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-chart-alerts-e2e-benchmark-tdd/01-PATTERNS.md
</context>

<tasks>

<task type="tracer">
  <name>End-to-end "Chart Alerts E2E Test" — create failing benchmark test</name>
  <files>tests/e2e/chart-alerts.spec.ts</files>
  <action>Create `tests/e2e/chart-alerts.spec.ts`. Use Playwright patterns from `alerts.spec.ts` (state initialization, auth bypass) and `marker-placement.spec.ts` (chart API mocking) per user decisions. The test should navigate to the chart. Since Lightweight Charts uses an HTML5 `<canvas>` without DOM nodes for price levels, simulate hover and click using bounding box coordinate offsets relative to the canvas to target a specific price level on the Y-axis. Expect a plus (+) button to appear in the DOM, click it, and verify the alert creation flow opens with the pre-filled price. Continue the script to fill out the alert form, submit it, and assert the new alert is visually plotted on the chart or listed in the active alerts. **IMPORTANT: Do not attempt to fix the application code to make this test pass. The test will intentionally fail at the hover/button visibility step, as the UI is not yet built. This is the desired benchmark behavior per TEST-01.**</action>
  <verify>
    <automated>npx playwright test tests/e2e/chart-alerts.spec.ts 2>&1 | grep -iE "timeout|waiting for selector|locator"</automated>
  </verify>
  <done>Test is implemented with proper mocking and fails at the interaction step due to missing implementation.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Test Runner → App | Execution of E2E tests against the frontend dev server |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-01 | Spoofing | `tests/e2e/chart-alerts.spec.ts` | low | accept | E2E mock injections are constrained to the test environment only. |
</threat_model>

<verification>
- Verify that `npx playwright test tests/e2e/chart-alerts.spec.ts` executes and fails at the expected interaction step.
</verification>

<success_criteria>
- `tests/e2e/chart-alerts.spec.ts` exists and is fully fleshed out with mocking setup.
- The test accurately describes the TEST-01 requirements for hovering and clicking.
</success_criteria>

<output>
Create `.planning/phases/01-chart-alerts-e2e-benchmark-tdd/01-01-SUMMARY.md` when done
</output>
