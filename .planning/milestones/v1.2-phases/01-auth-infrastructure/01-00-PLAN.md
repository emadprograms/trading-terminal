---
phase: 01-auth-infrastructure
plan: 00
type: setup
wave: 0
depends_on: []
files_modified: [server/proxy.test.ts, src/hooks/useSession.test.ts, src/store/useSessionStore.test.ts, src/components/EnvToggle.test.tsx, src/components/AccountHeader.test.tsx, tests/setup.ts, package.json]
autonomous: true
requirements: [AUTH-01, AUTH-02, AUTH-03, DATA-03]

must_haves:
  truths:
    - "Test environment is configured with MSW for API mocking"
    - "Base test files exist for all critical components"
  artifacts:
    - path: "server/proxy.test.ts"
      provides: "Proxy integration tests"
    - path: "src/hooks/useSession.test.ts"
      provides: "Auth handshake tests"
    - path: "src/store/useSessionStore.test.ts"
      provides: "Session store unit tests"
    - path: "src/components/EnvToggle.test.tsx"
      provides: "UI state tests"
    - path: "src/components/AccountHeader.test.tsx"
      provides: "Account data display tests"
---

<objective>
Setup the test scaffolding and mocking infrastructure for Phase 1. This ensures that all subsequent implementation waves can be verified with automated tests.

Purpose: Enable TDD and automated verification.
Output: Test files and MSW configuration.
</objective>

<execution_context>
@C:/Users/Emad/Documents/GitHub/trading-terminal/.gemini/get-shit-done/workflows/execute-plan.md
</execution_context>

<tasks>

<task type="auto">
  <name>Task 1: Configure MSW for Testing</name>
  <files>tests/setup.ts, package.json</files>
  <action>
    - Install `msw`.
    - Configure MSW in `tests/setup.ts` to intercept requests.
    - Setup base handlers for:
      1. `POST /session` (Mock Capital.com login response with headers).
      2. `GET /accounts` (Mock account state).
      3. `GET /ping` (Mock heartbeat).
  </action>
  <verify>
    <automated>npm test tests/setup.ts</automated>
  </verify>
  <done>MSW is configured and ready to mock API calls.</done>
</task>

<task type="auto">
  <name>Task 2: Create Session Store Tests</name>
  <files>src/store/useSessionStore.test.ts</files>
  <action>
    - Create a test file for the Zustand store.
    - Implement tests for:
      1. Initial state (null tokens).
      2. Setting tokens (CST, X-SECURITY-TOKEN).
      3. Environment switching (DEMO -> LIVE).
      4. Clearing state (logout).
  </action>
  <verify>
    <automated>npm test src/store/useSessionStore.test.ts</automated>
  </verify>
  <done>Session store tests are ready (failing initially).</done>
</task>

<task type="auto">
  <name>Task 3: Create Hook, Proxy & UI Test Scaffolding</name>
  <files>src/hooks/useSession.test.ts, server/proxy.test.ts, src/components/EnvToggle.test.tsx, src/components/AccountHeader.test.tsx</files>
  <action>
    - Create `src/hooks/useSession.test.ts` with basic handshake mocks.
    - Create `server/proxy.test.ts` to test Hono routing and header pass-through.
    - Create `src/components/EnvToggle.test.tsx` for UI state tests.
    - Create `src/components/AccountHeader.test.tsx` for account data display tests.
    - All tests should fail initially (TDD Red phase).
  </action>
  <verify>
    <automated>npm test src/hooks/useSession.test.ts server/proxy.test.ts src/components/EnvToggle.test.tsx src/components/AccountHeader.test.tsx</automated>
  </verify>
  <done>Test scaffolding for hooks, proxy, and UI components is in place.</done>
</task>

</tasks>

<success_criteria>
- All critical paths have failing tests ready for implementation.
</success_criteria>

<output>
Create `.planning/phases/01-auth-infrastructure/01-00-SUMMARY.md` when done
</output>
