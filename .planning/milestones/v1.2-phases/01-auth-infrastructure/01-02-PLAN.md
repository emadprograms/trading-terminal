---
phase: 01-auth-infrastructure
plan: 02
type: execute
wave: 2
depends_on: [01-01]
files_modified: [src/store/useSessionStore.ts, src/api/client.ts, src/hooks/useSession.ts, package.json]
autonomous: false
requirements: [AUTH-02]
must_haves:
  truths:
    - "CST and X-SECURITY-TOKEN are captured from the login response"
    - "Tokens are stored in-memory (Zustand) and never persisted to disk"
    - "Subsequent API calls automatically include the session tokens"
    - "Auth handshake events are logged using [StabilityTrace] format"
  artifacts:
    - path: "src/store/useSessionStore.ts"
      provides: "In-memory token management"
    - path: "src/api/client.ts"
      provides: "Configured Ky client"
    - path: "src/hooks/useSession.ts"
      provides: "Auth handshake logic"
---

<objective>
Implement the authentication handshake and session management logic on the frontend. This ensures the terminal can securely acquire and use Capital.com session tokens.

Purpose: Automate the dual-token handshake and handle session lifecycle.
Output: Working auth hook and API client.
</objective>

<execution_context>
@C:/Users/Emad/Documents/GitHub/trading-terminal/.gemini/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-auth-infrastructure/01-CONTEXT.md
@.planning/phases/01-auth-infrastructure/01-RESEARCH.md
@.planning/phases/01-auth-infrastructure/01-01-SUMMARY.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <what-built>Package legitimacy audit check</what-built>
  <how-to-verify>
    Verify that `ky`, `zustand`, and `@tanstack/react-query` are reputable packages.
    1. Visit npmjs.com/package/ky
    2. Visit npmjs.com/package/zustand
    3. Visit npmjs.com/package/@tanstack/react-query
    Confirm they have high downloads and no major security advisories.
  </how-to-verify>
  <resume-signal>approved</resume-signal>
</task>

<task type="auto" tdd="true">
  <name>Task 1: Create Session Store</name>
  <files>src/store/useSessionStore.ts</files>
  <behavior>
    - Initial state has null tokens and environment set to 'DEMO'
    - setTokens() updates cst and securityToken in-memory
    - clearTokens() resets tokens to null
    - environment toggle persists in-memory only
  </behavior>
  <action>
    - Install `zustand`.
    - Implement `useSessionStore` to hold:
      1. `cst`: string | null
      2. `securityToken`: string | null
      3. `proxyUrl`: string | null (to be set from discovery)
      4. `environment`: 'DEMO' | 'LIVE'
      5. `isAuthenticated`: boolean
    - Ensure tokens are NOT persisted (no `persist` middleware per D-05).
    - Clear state on page refresh (per D-06).
  </action>
  <verify>
    <automated>npm test src/store/useSessionStore.test.ts</automated>
  </verify>
  <done>Session store manages tokens and environment state in-memory.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Configure Ky Client</name>
  <files>src/api/client.ts</files>
  <behavior>
    - Request to /session intercepts CST and X-SECURITY-TOKEN from headers and saves to store
    - Non-session requests inject CST and X-SECURITY-TOKEN from store into headers
    - Uses proxyUrl as prefixUrl
  </behavior>
  <action>
    - Install `ky`.
    - Create a `ky` instance with:
      1. `prefixUrl` pointing to `proxyUrl` from the store.
      2. `beforeRequest` hook to inject `CST` and `X-SECURITY-TOKEN` from the store.
      3. `afterResponse` hook to capture `CST` and `X-SECURITY-TOKEN` from headers during the `/session` call (per Research Pattern 1).
  </action>
  <verify>
    <automated>npm test src/hooks/useSession.test.ts</automated>
  </verify>
  <done>Ky client automatically handles session header orchestration.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Implement useSession Hook</name>
  <files>src/hooks/useSession.ts</files>
  <behavior>
    - login() triggers POST /session and returns success/failure
    - ping() sends GET /ping (or /accounts) every 5 minutes to keep session alive
    - StabilityTrace logs are emitted for lifecycle events
  </behavior>
  <action>
    - Install `@tanstack/react-query`.
    - Create `useSession` hook.
    - Implement:
      1. `login()` function that calls `POST /session` via the proxy.
      2. `logout()` function that clears the store.
      3. `ping()` background heartbeat to keep the session alive (per Pitfall 2).
      4. Log login success/failure and ping events using `[StabilityTrace]` prefix (per D-09).
  </action>
  <verify>
    <automated>npm test src/hooks/useSession.test.ts</automated>
  </verify>
  <done>Session hook orchestrates the full auth lifecycle with stability tracing.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| Zustand -> Memory | Private app state |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-03 | Information Disclosure | LocalStorage | mitigate | Strictly enforce no persistence for session tokens (D-05). |
| T-01-SC | Tampering | npm installs | mitigate | slopcheck + blocking human checkpoint for ky, zustand, @tanstack/react-query. |
</threat_model>

<verification>
- Store state updates correctly on login.
- Ky requests include the expected headers.
- Session remains active through heartbeat pings.
</verification>

<success_criteria>
- Dual-token handshake is fully automated.
- Session tokens are securely managed in RAM.
</success_criteria>

<output>
Create `.planning/phases/01-auth-infrastructure/01-02-SUMMARY.md` when done
</output>
