---
phase: 01-auth-infrastructure
plan: 01
type: execute
wave: 1
depends_on: [01-00]
files_modified: [server/index.ts, .github/workflows/auth-proxy.yml, .planning/STATE.md, package.json]
autonomous: false
requirements: [AUTH-01]
must_haves:
  truths:
    - "Hono proxy is reachable and forwards requests to Capital.com"
    - "GHA workflow successfully starts the tunnel and proxy"
    - "Proxy URL is discoverable by the frontend"
    - "Proxy logs handshake events using [StabilityTrace] format"
  artifacts:
    - path: "server/index.ts"
      provides: "Backend proxy logic"
    - path: ".github/workflows/auth-proxy.yml"
      provides: "Ephemeral infrastructure orchestration"
  key_links:
    - from: "GHA runner"
      to: "Capital.com API"
      via: "Hono fetch"
---

<objective>
Setup the ephemeral backend infrastructure using Hono and GitHub Actions. This provides a secure bridge for the frontend to communicate with Capital.com without exposing secrets.

Purpose: Securely handle credentials and proxy requests.
Output: Working GHA proxy with Cloudflare Tunnel.
</objective>

<execution_context>
@C:/Users/Emad/Documents/GitHub/trading-terminal/.gemini/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-auth-infrastructure/01-CONTEXT.md
@.planning/phases/01-auth-infrastructure/01-RESEARCH.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <what-built>Package legitimacy audit check</what-built>
  <how-to-verify>
    Verify that `hono` and `@hono/node-server` are reputable packages.
    1. Visit https://www.npmjs.com/package/hono
    2. Visit https://www.npmjs.com/package/@hono/node-server
    Confirm they have high downloads and no major security advisories.
  </how-to-verify>
  <resume-signal>approved</resume-signal>
</task>

<task type="auto" tdd="true">
  <name>Task 1: Implement Hono Proxy</name>
  <files>server/index.ts, package.json</files>
  <behavior>
    - GET /ping returns 200 "pong"
    - POST /session forwards Body to Capital.com and exposes CST/X-SECURITY-TOKEN headers
    - Proxy middleware injects CST/X-SECURITY-TOKEN from headers into outgoing requests
    - All proxied requests use the target URL determined by ENV (DEMO/LIVE)
  </behavior>
  <action>
    - Install `hono` and `@hono/node-server`.
    - Create `server/index.ts`.
    - Implement a Hono app that:
      1. Enables CORS for the frontend origin (per Research Pattern 1).
      2. Exposes `CST` and `X-SECURITY-TOKEN` headers in CORS configuration.
      3. Provides a POST `/session` endpoint that forwards credentials from ENV to Capital.com.
      4. Provides a generic proxy handler that forwards all other requests to Capital.com API, injecting session headers from the incoming request.
      5. Uses `demo-api-capital.backend-capital.com` or `api-capital.backend-capital.com` based on an `ENV` flag.
      6. Logs proxy startup and request events using `[StabilityTrace]` prefix.
  </action>
  <verify>
    <automated>npm test server/proxy.test.ts</automated>
  </verify>
  <done>Hono proxy correctly handles session creation and generic request proxying with header exposure and tracing.</done>
</task>

<task type="auto">
  <name>Task 2: Create GHA Proxy Workflow</name>
  <files>.github/workflows/auth-proxy.yml</files>
  <action>
    - Create a workflow triggered by `workflow_dispatch`.
    - Steps:
      1. Checkout code.
      2. Install dependencies.
      3. Install `cloudflared` (per D-01).
      4. Start Hono server in background.
      5. Start `cloudflared tunnel --url http://localhost:3000`.
      6. Capture the `trycloudflare.com` URL from logs.
      7. Update a "discovery" artifact or environment variable (using `gh` CLI).
  </action>
  <verify>
    <automated>gh workflow run auth-proxy.yml --field env=DEMO</automated>
  </verify>
  <done>GHA workflow runs, starts the proxy, and exposes it via a public tunnel URL.</done>
</task>

<task type="auto">
  <name>Task 3: Automated Discovery Setup</name>
  <files>.planning/STATE.md</files>
  <action>
    - Add a mechanism to the GHA workflow (Task 2) to update `.planning/STATE.md` with the new tunnel URL (per D-02).
    - Use `gh api` to update the file in the repository or a similar mechanism if running in GHA.
    - Note: For local execution, the user may need to manually update if `gh` CLI cannot push.
  </action>
  <verify>
    <automated>grep -i "proxy_url" .planning/STATE.md</automated>
  </verify>
  <done>Active proxy URL is easily discoverable after infrastructure launch.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| Frontend -> Proxy | Untrusted browser input; CORS restricted |
| Proxy -> Capital.com | Secure server-to-server with secrets |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | Information Disclosure | Proxy Logs | mitigate | Do not log request bodies containing credentials or tokens (per D-09). |
| T-01-02 | Elevation of Privilege | Hono Proxy | mitigate | Restrict proxy targets to whitelist of Capital.com domains. |
| T-01-SC | Tampering | npm installs | mitigate | slopcheck + blocking human checkpoint for hono, @hono/node-server. |
</threat_model>

<verification>
- GHA workflow completes successfully.
- Tunnel URL is reachable via browser/curl.
- Proxy returns 401/404 correctly when no credentials provided.
</verification>

<success_criteria>
- Backend infrastructure is fully automated and ephemeral.
- Frontend has a clear path to discover the proxy endpoint.
</success_criteria>

<output>
Create `.planning/phases/01-auth-infrastructure/01-01-SUMMARY.md` when done
</output>
