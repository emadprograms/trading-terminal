---
phase: 03-order-execution-layer
plan: 00
type: execute
wave: 1
depends_on: []
files_modified: [src/api/client.ts, src/types/trade.ts, src/api/trade.ts, src/store/useTradeStore.ts, src/api/client.test.ts, src/api/trade.test.ts, src/store/useTradeStore.test.ts]
autonomous: true
requirements: [EXEC-01, EXEC-02]
must_haves:
  truths:
    - "User sees a 'Pending' status badge immediately after clicking Buy/Sell"
    - "The terminal uses the correct environment (Demo/Live) for the trade request"
    - "The store records the transaction attempt before the server responds"
  artifacts:
    - path: "src/api/client.ts"
      provides: "Fixed ky client with prefixUrl"
    - path: "src/store/useTradeStore.ts"
      provides: "State machine for PENDING orders"
    - path: "src/api/trade.test.ts"
      provides: "Trade API unit tests"
---

<objective>
Fix the API infrastructure and implement the core trade state machine foundation. This establishes the "Command" part of the execution flow.

Purpose: Fix implementation gaps and provide a solid foundation for trade state management.
Output: Working ky client, verified types, and a state-aware trade store.
</objective>

<execution_context>
@/Users/emadarshadalam/Documents/GitHub/trading-terminal/.gemini/gsd-core/workflows/execute-plan.md
@/Users/emadarshadalam/Documents/GitHub/trading-terminal/.gemini/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/03-order-execution-layer/03-CONTEXT.md
@.planning/phases/03-order-execution-layer/RESEARCH.md
@src/api/client.ts
@src/types/trade.ts
@src/store/useTradeStore.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Test Scaffolding & Types</name>
  <files>src/api/client.test.ts, src/api/trade.test.ts, src/store/useTradeStore.test.ts, src/types/trade.ts</files>
  <action>
    1. Create test file skeletons (using Vitest) for `client.test.ts`, `trade.test.ts`, and `useTradeStore.test.ts`.
    2. Review and update `src/types/trade.ts`:
       - Ensure `OrderStatus` includes `PENDING`, `ACCEPTED`, `REJECTED`.
       - Add `dealReference`, `dealId`, `workingOrderId` fields clearly.
       - Ensure Stop Loss types include `guaranteedStop: boolean` (per UI-02 requirement).
       - Add `ofr` (Offer/Ask) and `bid` price fields to relevant types for slippage tracking.
  </action>
  <verify>
    <automated>ls src/api/client.test.ts src/api/trade.test.ts src/store/useTradeStore.test.ts</automated>
  </verify>
  <done>Test skeletons created; Trade types match schema including guaranteedStop.</done>
</task>

<task type="auto">
  <name>Task 2: Fix ky Client & Implement Trade API</name>
  <files>src/api/client.ts, src/api/trade.ts</files>
  <action>
    1. Update `src/api/client.ts`:
       - Change `prefix` to `prefixUrl: 'api'`.
       - Ensure `beforeRequest` hook correctly sets 'x-env' header from session state (per D-03).
    2. Implement/Refine `src/api/trade.ts`:
       - `placeMarketOrder`: POST to `/positions`.
       - `placeLimitOrder`: POST to `/workingorders`.
       - `getConfirmation`: GET to `/confirms/{dealReference}`.
       - Ensure all methods return the `dealReference` string.
  </action>
  <verify>
    <automated>npm run test src/api/trade.test.ts</automated>
  </verify>
  <done>ky client fixed; Trade API implemented with unit test verification.</done>
</task>

<task type="auto">
  <name>Task 3: Implement Core Trade Store (State Machine Foundation)</name>
  <files>src/store/useTradeStore.ts</files>
  <action>
    Rewrite `src/store/useTradeStore.ts` to implement the foundation of the state machine:
    - State: `pendingOrders` (Record of dealRef -> Order), `positions` (Array), `workingOrders` (Array).
    - Action `placeOrder`:
      1. Call `tradeApi.placeMarketOrder` or `placeLimitOrder`.
      2. Add to `pendingOrders` with status `PENDING` using the returned `dealReference`.
    - Action `handleApiError`: Centralized error handling to transition `PENDING` -> `REJECTED`.
  </action>
  <verify>
    <automated>npm run test src/store/useTradeStore.test.ts</automated>
  </verify>
  <done>useTradeStore correctly records PENDING state and transitions on API error.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| UI -> Store | Order params (size, epic) |
| Store -> API | Trade commands |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01 | Tampering | src/api/trade.ts | mitigate | Validate order size and Epic format before sending |
| T-03-02 | Denial of Service | UI | mitigate | Debounce/Disable trade buttons immediately on click |
</threat_model>

<verification>
Run vitest on the created test files.
</verification>

<success_criteria>
1. `src/api/client.ts` uses `prefixUrl`.
2. `useTradeStore` successfully places a "pending" order in state.
3. Unit tests pass for client, trade API, and store foundation.
</success_criteria>

<output>
Create `.planning/phases/03-order-execution-layer/03-00-SUMMARY.md` when done
</output>
