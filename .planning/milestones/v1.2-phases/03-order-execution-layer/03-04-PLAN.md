---
phase: 03-order-execution-layer
plan: 04
type: execute
wave: 5
depends_on: [03-03]
files_modified: [src/store/useTradeStore.ts, src/store/useTradeStore.test.ts, src/api/trade.ts, src/lib/api-utils.ts, src/lib/api-utils.test.ts]
autonomous: true
requirements: [EXEC-01, EXEC-02, EXEC-03, UI-02]
gap_closure: true

must_haves:
  truths:
    - "Limit/Stop orders are sent to /v1/workingorders"
    - "Market orders are sent to /v1/positions"
    - "User can retry trading immediately if an order fails (UI re-enables)"
    - "guaranteedStop is only true if selected by the user"
    - "Error messages do not reveal proxy hostnames or localhost URLs"
  artifacts:
    - path: "src/store/useTradeStore.ts"
      provides: "Correct order routing and state recovery"
    - path: "src/store/useTradeStore.test.ts"
      provides: "Verification for routing and error recovery"
    - path: "src/api/trade.ts"
      provides: "Flexible order parameters for correct routing"
    - path: "src/lib/api-utils.ts"
      provides: "Aggressive error sanitization"
    - path: "src/lib/api-utils.test.ts"
      provides: "Verification for domain-agnostic sanitization"
  key_links:
    - from: "src/store/useTradeStore.ts"
      to: "src/api/trade.ts"
      via: "tradeApi import"
      pattern: "import.*tradeApi.*from.*api/trade"
    - from: "src/api/trade.ts"
      to: "src/lib/api-utils.ts"
      via: "error handling integration"
      pattern: "import.*from.*lib/api-utils"
---

<objective>
Fix critical execution gaps identified during UAT:
1. Routing: Send Limit/Stop orders to the correct endpoint.
2. Robustness: Ensure the UI doesn't hang (isExecuting) on API errors.
3. Accuracy: Respect user's Guaranteed Stop preference.
4. Security: Sanitize all URLs from error messages.
</objective>

<execution_context>
@C:/Users/Emad/Documents/GitHub/trading-terminal/.gemini/gsd-core/workflows/execute-plan.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/03-order-execution-layer/03-UAT.md
@src/store/useTradeStore.ts
@src/api/trade.ts
@src/lib/api-utils.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix Routing and State in useTradeStore.ts</name>
  <files>src/store/useTradeStore.ts, src/store/useTradeStore.test.ts</files>
  <action>
    - Update `TradeState['placeOrder']` type definition to accept `MarketOrderParams | LimitOrderParams`.
    - In `placeOrder` implementation:
      - Check if `params.type` is 'LIMIT' or 'STOP'.
      - If it is a working order (LIMIT/STOP), call `tradeApi.placeLimitOrder(params as LimitOrderParams)`.
      - If it is a MARKET order, call `tradeApi.placeMarketOrder(params as MarketOrderParams)`.
      - Remove the hardcoded `guaranteedStop: true`; use `params.guaranteedStop ?? false`.
      - Add a `catch (error)` block to the outer `try` (or wrap the API call).
      - Inside `catch`, ensure `set({ isExecuting: false })` is called before re-throwing.
    - Update `src/store/useTradeStore.test.ts` to include tests for routing (placeLimitOrder vs placeMarketOrder) and state recovery (isExecuting) after errors.
  </action>
  <verify>
    <automated>npm test src/store/useTradeStore.test.ts</automated>
    <human-check>Confirm that fixing order placement unblocks the Flatten feature (EXEC-03) as identified in UAT Test 6.</human-check>
  </verify>
  <done>Limit orders call placeLimitOrder, Market orders call placeMarketOrder, and UI unlocks on error (verified unblocking EXEC-03).</done>
</task>

<task type="auto">
  <name>Task 2: Update tradeApi and api-utils for Robustness</name>
  <files>src/api/trade.ts, src/lib/api-utils.ts, src/lib/api-utils.test.ts</files>
  <action>
    - In `src/api/trade.ts`:
      - Update `MarketOrderParams` to optionally include `type?: OrderType`.
      - Ensure `placeMarketOrder` and `placeLimitOrder` handle their respective types.
    - In `src/lib/api-utils.ts`:
      - Update `sanitizeErrorMessage` regex to be domain-agnostic.
      - Use `const proxyPattern = /https?:\/\/[a-zA-Z0-9][-a-zA-Z0-9]{0,62}(\.[a-zA-Z0-9][-a-zA-Z0-9]{0,62})+(:[0-9]{1,5})?(\/[^\s]*)?/gi;` or a simpler aggressive one like `https?:\/\/[^\s]*/gi`.
      - Ensure it strips everything from the protocol to the end of the URL string.
    - Update `src/lib/api-utils.test.ts` to verify the domain-agnostic sanitization (stripping localhost, internal domains, and aggressive proxy patterns).
  </action>
  <verify>
    <automated>npm test src/lib/api-utils.test.ts</automated>
  </verify>
  <done>API parameters are flexible and error messages are fully sanitized (no localhost or internal domains).</done>
</task>

</tasks>

<success_criteria>
- UAT Gap 1 (Routing) is closed: Limit orders no longer go to /positions.
- UAT Gap 2 (isExecuting hang) is closed: UI buttons re-enable after an error.
- UAT Gap 3 (guaranteedStop forced) is closed: Guaranteed Stop is optional.
- UAT Gap 4 (Sanitization leak) is closed: No URLs visible in toasts.
- EXEC-03 (Flatten) is unblocked by working order placement.
</success_criteria>

<output>
Create `.planning/phases/03-order-execution-layer/03-04-SUMMARY.md` when done
</output>
