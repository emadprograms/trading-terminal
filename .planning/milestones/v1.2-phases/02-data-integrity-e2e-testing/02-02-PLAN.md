---
phase: 02-data-integrity-e2e-testing
plan: 02
type: execute
wave: 2
depends_on: ["02-01"]
files_modified: ["tests/e2e/critical-path.spec.ts", "tests/e2e/stress-test.ts"]
autonomous: true
requirements: [TEST-02]

must_haves:
  truths:
    - "Automated tests successfully simulate placing orders, switching charts, and viewing histories"
    - "Tests validate that data stitching perfectly completes without regressions"
    - "Stress test script successfully runs high-frequency requests against demo API to map limitations"
  artifacts:
    - path: "tests/e2e/critical-path.spec.ts"
      provides: "E2E UI interaction test against real Vercel URL"
    - path: "tests/e2e/stress-test.ts"
      provides: "High-frequency load test"
  key_links:
    - from: "tests/e2e/critical-path.spec.ts"
      to: "Vercel URL"
      via: "Playwright page.goto"
      pattern: "page\\.goto"
---

<objective>
Write the Playwright E2E suite to test the critical path and the stress-testing scripts to discover API limitations.

Purpose: To guarantee state integrity through an automated critical path and map out the Capital.com demo API bounds without relying on mocks.
Output: Comprehensive E2E test files and stress testing scripts.
</objective>

<execution_context>
@.agent/gsd-core/workflows/execute-plan.md
@.agent/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-data-integrity-e2e-testing/02-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build Critical Path E2E Suite</name>
  <files>tests/e2e/critical-path.spec.ts</files>
  <action>Create a Playwright test suite `critical-path.spec.ts` that hits the deployed Vercel URL. The test must simulate a user placing an order, switching charts, and viewing order history. Assert that the "Data Stitching Error" banner is NOT present (indicating perfectly contiguous REST/WS data) and that there are no regressions in correctness. Do not include cleanup/teardown for orders in the demo account (D-08).</action>
  <verify>
    <automated>npx playwright test tests/e2e/critical-path.spec.ts --project=chromium</automated>
  </verify>
  <done>Playwright suite tests the full unmocked journey and verifies data stitching integrity.</done>
</task>

<task type="auto">
  <name>Task 2: Develop Stress-Testing Script</name>
  <files>tests/e2e/stress-test.ts</files>
  <action>Write a standalone script or Playwright test `stress-test.ts` to issue high-frequency requests to the Capital.com demo API (via the Vercel proxy) to discover real-world limitations like rate limits and erratic conditions (D-03). The results from this script should be logged so developers can apply the findings as rules in the main proxy later.</action>
  <verify>
    <automated>npx playwright test tests/e2e/stress-test.ts --project=chromium</automated>
  </verify>
  <done>A script exists that can be run to stress-test the demo API integration and document its bounds.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Client→API | Playwright E2E interactions with Capital.com demo |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-02 | Denial of Service | tests/e2e/stress-test.ts | mitigate | Ensure stress test is clearly marked and isolated so it doesn't accidentally run in CI pipelines against production |
| T-02-SC | Tampering | npm/pip/cargo installs | mitigate | slopcheck + blocking human checkpoint for [ASSUMED]/[SUS] |
</threat_model>

<verification>
Playwright tests should pass when run individually (note: tests might fail if Vercel URL isn't live during development, but they must be structurally sound and executable).
</verification>

<success_criteria>
E2E suite comprehensively tests the critical path against real endpoints and a stress test maps the proxy's boundaries.
</success_criteria>

<output>
Create `.planning/phases/02-data-integrity-e2e-testing/02-02-SUMMARY.md` when done
</output>
