---
phase: 02-data-integrity-e2e-testing
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: ["playwright.config.ts", "src/hooks/useChartData.ts", "src/components/StitchingErrorBanner.tsx", "src/components/ChartUnit.tsx"]
autonomous: true
requirements: [TEST-01]

must_haves:
  truths:
    - "Test environment accurately points to Vercel and Capital.com demo"
    - "Application detects and reports timestamp gaps between REST history and WebSocket ticks"
    - "User explicitly sees a 'Data Stitching Error' banner if data stitching fails"
  artifacts:
    - path: "playwright.config.ts"
      provides: "E2E config pointing to Vercel/demo"
    - path: "src/components/StitchingErrorBanner.tsx"
      provides: "UI element for stitching errors"
  key_links:
    - from: "src/hooks/useChartData.ts"
      to: "src/components/StitchingErrorBanner.tsx"
      via: "State or error boundary propagation"
      pattern: "DataStitchingError"
---

<objective>
Configure testing infrastructure to target the real demo API via Vercel and implement the UI layer for explicitly catching and displaying data stitching gaps.

Purpose: To ensure testing is authentic (no mocks) and to surface data inconsistencies strictly without silent failures per D-01, D-02, D-05, and D-07.
Output: Configured test runner, robust timestamp validation, and explicit error banner.
</objective>

<execution_context>
@.agent/gsd-core/workflows/execute-plan.md
@.agent/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-data-integrity-e2e-testing/02-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update Playwright Config</name>
  <files>playwright.config.ts</files>
  <action>Update playwright.config.ts to target the deployed Vercel URL instead of localhost. Configure it specifically to interact with the proxy hitting the Capital.com demo account. Ensure any MSW usage or mocking is completely bypassed for E2E tests (D-01, D-02, D-04).</action>
  <verify>
    <automated>npx playwright test --project=chromium</automated>
  </verify>
  <done>Playwright is configured to run against the live Vercel environment with demo credentials and no local mocks.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement Data Stitching Validation & Error Banner</name>
  <files>src/hooks/useChartData.ts, src/components/StitchingErrorBanner.tsx, src/components/ChartUnit.tsx</files>
  <behavior>
    - Test 1: Should calculate gap between REST history last timestamp and WS first tick timestamp.
    - Test 2: If gap exceeds acceptable threshold, should trigger DataStitchingError.
    - Test 3: StitchingErrorBanner should display "Data Stitching Error: [description] - [reason]" when error occurs.
  </behavior>
  <action>Implement timestamp validation in `useChartData.ts` (or relevant data hook) to perfectly match REST historical timestamps with WebSocket live ticks (D-05). If a gap is detected or the API fails unpredictably (D-06), trigger an explicit error state. Create `StitchingErrorBanner.tsx` and wire it into `ChartUnit.tsx` to render the exact format "Data Stitching Error: [Exact issue description] - [Why it happened]" (D-07). No silent failures.</action>
  <verify>
    <automated>npm run test -- --run</automated>
  </verify>
  <done>The application dynamically checks timestamp continuity and explicitly warns the user on screen if stitching fails.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Client→API | Playwright E2E interactions with Vercel deployment |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | Information Disclosure | playwright.config.ts | mitigate | Ensure demo credentials are used securely, no live account secrets hardcoded |
| T-02-SC | Tampering | npm/pip/cargo installs | mitigate | slopcheck + blocking human checkpoint for [ASSUMED]/[SUS] |
</threat_model>

<verification>
Ensure data stitching errors are correctly thrown and caught in UI without crashing the client.
</verification>

<success_criteria>
Playwright is fully configured for live Vercel testing and the app explicitly surfaces data stitching errors.
</success_criteria>

<output>
Create `.planning/phases/02-data-integrity-e2e-testing/02-01-SUMMARY.md` when done
</output>
